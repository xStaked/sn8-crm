---
phase: 02-bot-conversation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/app.module.ts
  - apps/backend/src/bot-conversation/bot-conversation.module.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.ts
  - apps/backend/src/bot-conversation/bot-conversation.repository.ts
  - apps/backend/src/bot-conversation/bot-conversation.types.ts
  - apps/backend/src/messaging/messaging.module.ts
autonomous: true
requirements:
  - BOT-04

must_haves:
  truths:
    - "The backend has a dedicated bot-conversation domain instead of burying FSM state inside MessageProcessor"
    - "Active conversation routing state lives in Redis with a 24-hour TTL and is reconstructible from a Prisma backup record"
    - "Bot routing state is keyed by stable conversation identity, not by transient webhook delivery ids"
    - "Phase 2 state remains separate from CommercialBrief and QuoteDraft so routing and sales-state do not collapse into one model"
  artifacts:
    - path: "apps/backend/prisma/schema.prisma"
      provides: "Durable backup model for bot conversation routing state"
      contains: "ConversationState"
    - path: "apps/backend/src/bot-conversation/bot-conversation.repository.ts"
      provides: "Redis-backed repository owning key naming, TTL, and reconstruction methods"
      contains: "bot:fsm:"
    - path: "apps/backend/src/bot-conversation/bot-conversation.types.ts"
      provides: "State enums and metadata contract for the FSM"
      contains: "BotConversationState"
  key_links:
    - from: "apps/backend/src/bot-conversation/bot-conversation.module.ts"
      to: "apps/backend/src/app.module.ts"
      via: "NestJS module import"
      pattern: "BotConversationModule"
    - from: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      to: "apps/backend/src/bot-conversation/bot-conversation.repository.ts"
      via: "FSM reads and writes route through one repository boundary"
      pattern: "loadState|saveState|rebuildState"
---

<objective>
Create the durable foundation for the bot conversation engine: state contracts, Redis primary storage, Prisma backup state, and the dedicated backend module that will own routing behavior.

Purpose: make Phase 2 a real conversation-state system instead of a collection of ad hoc conditionals in the message worker.
Output: bot-conversation module, durable state model, Redis repository with TTL semantics, and a service entry point ready to receive inbound messages in later plans.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bot-conversation-engine/02-CONTEXT.md
@.planning/phases/02-bot-conversation-engine/02-RESEARCH.md

<interfaces>
Current ingress worker behavior:

From apps/backend/src/messaging/processors/message.processor.ts:
```typescript
const replyPlan = await this.conversationFlowService.planReply({
  conversationId: normalized.fromPhone,
  inboundMessageId: normalized.externalMessageId,
  inboundBody: normalized.body,
});
```

Current Redis availability:

From apps/backend/src/redis/redis.module.ts:
```typescript
@Global()
@Module({
  providers: [{ provide: REDIS_CLIENT, useFactory: (...) => new Redis(...) }],
  exports: [REDIS_CLIENT],
})
```

Current durable models already used by downstream AI-sales:

From apps/backend/prisma/schema.prisma:
```prisma
model CommercialBrief { ... }
model QuoteDraft { ... }
```

There is no bot routing state model or repository today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a durable backup model for bot routing state</name>
  <files>
    apps/backend/prisma/schema.prisma
  </files>
  <read_first>
    apps/backend/prisma/schema.prisma
    .planning/phases/02-bot-conversation-engine/02-CONTEXT.md
    .planning/phases/02-bot-conversation-engine/02-RESEARCH.md
  </read_first>
  <action>
1. Extend Prisma with a dedicated bot routing state model. The exact names may vary, but it must cover:
   - stable `conversationId`
   - current FSM state
   - metadata JSON for state-specific context
   - off-flow retry count
   - `lastTransitionAt`
   - `expiresAt`
2. Keep this model focused on routing state only. Do not copy commercial brief fields from `CommercialBrief`.
3. Add the minimum indexes/uniques required for stable lookup by conversation identity and time-based cleanup/reporting.
4. Preserve existing AI-sales models unchanged except where a relation or naming clarification is truly needed.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && pnpm exec prisma validate</automated>
  </verify>
  <done>
    - Bot conversation state can survive Redis loss
    - Routing state is queryable without scanning transcript history
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the bot-conversation repository and service contracts</name>
  <files>
    apps/backend/src/bot-conversation/bot-conversation.module.ts
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/bot-conversation.repository.ts
    apps/backend/src/bot-conversation/bot-conversation.types.ts
    apps/backend/src/app.module.ts
    apps/backend/src/messaging/messaging.module.ts
  </files>
  <read_first>
    apps/backend/src/app.module.ts
    apps/backend/src/messaging/messaging.module.ts
    apps/backend/src/redis/redis.module.ts
    apps/backend/src/messaging/processors/message.processor.ts
    .planning/phases/02-bot-conversation-engine/02-RESEARCH.md
  </read_first>
  <action>
1. Create a dedicated `bot-conversation` module with a service + repository boundary.
2. Define the FSM state enum and metadata types for at least:
   - `GREETING`
   - `INFO_SERVICES`
   - `QUALIFYING`
   - `HUMAN_HANDOFF`
   - `AI_SALES`
3. Implement repository methods for:
   - load current state from Redis
   - write state to Redis with 24-hour TTL
   - mirror transitions into Prisma backup
   - rebuild Redis state from Prisma when Redis misses
4. Wire the module into the NestJS app so later plans can call it from `MessageProcessor`.
5. Stop short of changing the worker routing logic in this plan; deliver the contracts and plumbing first.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - There is a real home for conversation routing logic
    - State storage semantics are centralized before behavior is added
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec prisma validate` passes with the new bot routing state model
2. `pnpm exec tsc --noEmit` passes with the new bot-conversation module wired into the backend
3. The codebase contains:
   - a durable `ConversationState`-style model
   - a Redis-backed repository with 24-hour TTL
   - a bot-conversation service that exposes the future routing boundary
</verification>

<success_criteria>
- BOT-04 foundation exists: conversation routing state can persist beyond a single inbound message
- The repo is ready to add greeting and routing behavior without storing FSM logic in the worker itself
</success_criteria>
