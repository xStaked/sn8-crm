---
phase: 02-bot-conversation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/backend/src/channels/channel.adapter.ts
  - apps/backend/src/channels/kapso/kapso.client.ts
  - apps/backend/src/channels/kapso/kapso.adapter.ts
  - apps/backend/src/channels/kapso/kapso.adapter.spec.ts
  - apps/backend/src/channels/kapso/normalized-message.interface.ts
  - apps/backend/src/messaging/messaging.service.ts
  - apps/backend/src/messaging/processors/message.processor.ts
  - apps/backend/src/bot-conversation/bot-conversation.module.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.ts
  - apps/backend/src/bot-conversation/human-handoff.service.ts
  - apps/backend/src/bot-conversation/prompts/greeting-messages.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
autonomous: true
requirements:
  - BOT-01
  - BOT-04

must_haves:
  truths:
    - "A first-contact or expired conversation receives the Phase 2 greeting/menu instead of falling directly into qualification copy"
    - "The greeting uses interactive reply buttons when supported by the channel integration rather than pretending a menu exists only in prose"
    - "Inbound button taps normalize into an explicit routing contract instead of being treated as ambiguous free text"
    - "Returning conversations inside the active window resume their prior state and do not get the greeting repeated"
    - "HUMAN_HANDOFF notifies the owner through a dedicated handoff notifier that reuses the existing WhatsApp persistence pattern without coupling the bot module to quote-review APIs"
  artifacts:
    - path: "apps/backend/src/channels/channel.adapter.ts"
      provides: "Outbound contract capable of sending Phase 2 greeting buttons"
      contains: "sendInteractive"
    - path: "apps/backend/src/channels/kapso/normalized-message.interface.ts"
      provides: "Inbound normalization contract with message kind and interactive reply metadata"
      contains: "messageType"
    - path: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      provides: "Greeting, INFO_SERVICES, and HUMAN_HANDOFF routing behavior"
      contains: "handleInbound"
    - path: "apps/backend/src/messaging/processors/message.processor.ts"
      provides: "Worker entry point now routed through bot-conversation instead of directly through ConversationFlowService"
      contains: "botConversationService"
  key_links:
    - from: "apps/backend/src/messaging/processors/message.processor.ts"
      to: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      via: "all auto-reply routing goes through the FSM service"
      pattern: "handleInbound"
    - from: "apps/backend/src/bot-conversation/human-handoff.service.ts"
      to: "apps/backend/src/messaging/messaging.service.ts"
      via: "handoff owner notification uses the standard outbound messaging path"
      pattern: "sendText|message.create"
---

<objective>
Implement the Phase 2 greeting/menu experience and route inbound messages through the new bot-conversation service instead of directly through `ConversationFlowService`.

Purpose: satisfy the first-contact UX and make state-aware routing real.
Output: interactive greeting support, worker-to-FSM integration, INFO_SERVICES/HUMAN_HANDOFF entry behavior, and resume-vs-regreet logic based on active/expired state.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bot-conversation-engine/02-CONTEXT.md
@.planning/phases/02-bot-conversation-engine/02-RESEARCH.md
@.planning/phases/02-bot-conversation-engine/02-01-PLAN.md

<interfaces>
Current outbound channel contract:

From apps/backend/src/channels/channel.adapter.ts:
```typescript
abstract sendText(to: string, body: string, senderPhoneNumberId?: string): Promise<string>;
abstract sendTemplate(to: string, templateName: string, params: string[]): Promise<void>;
```

Current normalized inbound contract:

From apps/backend/src/channels/kapso/normalized-message.interface.ts:
```typescript
export interface NormalizedMessage {
  externalMessageId: string;
  direction: 'inbound' | 'outbound';
  fromPhone: string;
  toPhone: string;
  body: string | null;
  channel: string;
  rawPayload: unknown;
}
```

Current worker behavior:

From apps/backend/src/messaging/processors/message.processor.ts:
```typescript
if (!this.shouldAutoReply(normalized)) {
  return;
}
// ... direct call into conversationFlowService.planReply(...)
```

Locked greeting requirements from phase context:
- 3 options: "Cotizar proyecto" / "Conocer servicios" / "Hablar con alguien"
- returning active conversations resume in place
- expired conversations receive "Hola de nuevo" + same options
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the channel contracts for interactive greeting buttons and inbound button normalization</name>
  <files>
    apps/backend/src/channels/channel.adapter.ts
    apps/backend/src/channels/kapso/kapso.client.ts
    apps/backend/src/channels/kapso/kapso.adapter.ts
    apps/backend/src/channels/kapso/kapso.adapter.spec.ts
    apps/backend/src/channels/kapso/normalized-message.interface.ts
    apps/backend/src/messaging/messaging.service.ts
  </files>
  <read_first>
    apps/backend/src/channels/channel.adapter.ts
    apps/backend/src/channels/kapso/kapso.client.ts
    apps/backend/src/channels/kapso/kapso.adapter.ts
    apps/backend/src/channels/kapso/kapso.adapter.spec.ts
    apps/backend/src/channels/kapso/normalized-message.interface.ts
    apps/backend/src/messaging/messaging.service.ts
    .planning/phases/02-bot-conversation-engine/02-RESEARCH.md
  </read_first>
  <action>
1. Add the smallest stable outbound contract needed for Phase 2 interactive greetings, such as `sendInteractiveButtons(...)`.
2. Implement the Kapso path required to send reply buttons with the three locked options.
3. Expand the normalized inbound contract so Phase 2 can distinguish:
   - interactive button taps
   - plain text
   - non-text/media payloads
4. Normalize the inbound metadata the router will need, such as message kind plus interactive reply id/title.
5. Keep the abstraction narrow: Phase 2 needs greeting buttons and button-tap routing, not a generic visual flow builder.
6. If the SDK wrapper lacks a helper, add a targeted client method and document that decision in the summary instead of silently falling back to plain text.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && pnpm exec tsc --noEmit</automated>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest kapso.adapter.spec --runInBand</automated>
  </verify>
  <done>
    - The channel layer can honestly send the Phase 2 greeting UX
    - Greeting/menu behavior is no longer blocked by transport limitations
    - Button-tap routing data exists at the normalized ingress seam
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route first-contact, returning, info-services, and human-handoff messages through the FSM service</name>
  <files>
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/bot-conversation/bot-conversation.module.ts
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/human-handoff.service.ts
    apps/backend/src/bot-conversation/prompts/greeting-messages.ts
    apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
  </files>
  <read_first>
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/bot-conversation/bot-conversation.module.ts
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/channels/kapso/normalized-message.interface.ts
    apps/backend/src/ai-sales/owner-review.service.ts
    .planning/phases/02-bot-conversation-engine/02-CONTEXT.md
  </read_first>
  <action>
1. Replace the direct `conversationFlowService.planReply(...)` call in `MessageProcessor` with the bot-conversation service routing boundary.
2. Implement these entry behaviors:
   - new conversation -> GREETING with buttons
   - active conversation -> resume current state without repeated greeting
   - expired conversation -> personalized "Hola de nuevo" + buttons
   - INFO_SERVICES selection -> bounded informative response and state update
   - HUMAN_HANDOFF selection -> owner notification + customer wait message + state update
3. Persist every transition through the repository introduced in Plan 01.
4. Register any new handoff notifier/provider in `bot-conversation.module.ts` so the DI graph is complete in the same plan that introduces the service.
5. Implement HUMAN_HANDOFF notification through a dedicated notifier/service in the bot-conversation boundary that reuses the same outbound persistence pattern as owner review, but does not call `requestOwnerReview(...)`.
6. Add tests proving both button taps and active/expired resumes route correctly.
7. Continue to ignore owner command messages and empty inbound bodies at the worker guard level.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest bot-conversation.service.spec --runInBand</automated>
  </verify>
  <done>
    - BOT-01 is implemented at the routing layer
    - Resume/regreet behavior now depends on stored state rather than guesswork
  </done>
</task>

</tasks>

<verification>
1. The outbound channel abstraction can send the greeting buttons required by the approved context
2. The inbound normalized contract exposes button-tap routing data and message kind
3. `MessageProcessor` routes auto-replies through `BotConversationService`
4. First-touch, active resume, expired return, INFO_SERVICES, and HUMAN_HANDOFF flows all persist state transitions
</verification>

<success_criteria>
- BOT-01 is satisfied for the greeting/menu entry experience
- BOT-04 is advanced because active vs expired state now changes runtime behavior
</success_criteria>
