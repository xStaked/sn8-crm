---
phase: 02-bot-conversation-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - apps/backend/src/bot-conversation/bot-conversation.repository.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
  - apps/backend/src/messaging/processors/message.processor.spec.ts
  - apps/backend/test/bot-conversation.e2e-spec.ts
autonomous: true
requirements:
  - BOT-01
  - BOT-03
  - BOT-04

must_haves:
  truths:
    - "Redis loss or process restart does not force an in-progress lead back to the initial greeting"
    - "The 24-hour idle-expiry rule is enforced deterministically and changes the greeting behavior for returning leads"
    - "End-to-end worker coverage proves the bot conversation engine persists, routes, and recovers across multiple inbound turns"
    - "Outbound state transitions remain auditable through both Redis state and Prisma backup rows"
  artifacts:
    - path: "apps/backend/src/bot-conversation/bot-conversation.repository.ts"
      provides: "Redis reconstruction and expiry enforcement logic"
      contains: "rebuildState"
    - path: "apps/backend/src/bot-conversation/bot-conversation.service.spec.ts"
      provides: "Unit proof for resume, expiry, and off-flow recovery behavior"
      contains: "24 hours"
    - path: "apps/backend/test/bot-conversation.e2e-spec.ts"
      provides: "Worker-level proof that inbound turns preserve routing state across multiple messages"
      contains: "incoming-messages"
  key_links:
    - from: "apps/backend/src/bot-conversation/bot-conversation.repository.ts"
      to: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      via: "service recovers state from Prisma backup when Redis misses"
      pattern: "rebuildState"
    - from: "apps/backend/src/messaging/processors/message.processor.spec.ts"
      to: "apps/backend/test/bot-conversation.e2e-spec.ts"
      via: "unit and HTTP/queue coverage prove the same routing semantics"
      pattern: "message_auto_replied|incoming-messages"
---

<objective>
Harden Phase 2 with recovery and verification coverage: restart-safe continuation, 24-hour expiry behavior, and end-to-end proof that the bot state machine survives beyond a single process lifetime.

Purpose: close the gap between "state exists in code" and "state actually survives the operational cases the roadmap promises."
Output: Redis reconstruction logic, expiry tests, worker/e2e proof, and auditable persistence semantics for the full Phase 2 routing loop.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bot-conversation-engine/02-CONTEXT.md
@.planning/phases/02-bot-conversation-engine/02-RESEARCH.md
@.planning/phases/02-bot-conversation-engine/02-01-PLAN.md
@.planning/phases/02-bot-conversation-engine/02-02-PLAN.md
@.planning/phases/02-bot-conversation-engine/02-03-PLAN.md

<interfaces>
Locked persistence rules from context:
- Redis primary store with 24h TTL
- DB backup on every transition
- active conversations resume without repeated greeting
- expired conversations receive "Hola de nuevo" and restart from greeting

Current worker boundary:

From apps/backend/src/messaging/processors/message.processor.ts:
```typescript
@Processor('incoming-messages')
export class MessageProcessor extends WorkerHost {
  async process(job: Job): Promise<void> {
    // normalize -> persist inbound -> route -> send outbound
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Redis-miss reconstruction and deterministic expiry semantics</name>
  <files>
    apps/backend/src/bot-conversation/bot-conversation.repository.ts
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
  </files>
  <read_first>
    apps/backend/src/bot-conversation/bot-conversation.repository.ts
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
    .planning/phases/02-bot-conversation-engine/02-CONTEXT.md
  </read_first>
  <action>
1. Implement and test the repository/service behavior for:
   - Redis hit -> resume active state
   - Redis miss + unexpired Prisma backup -> rebuild Redis and resume
   - expired state -> reset to greeting path with "Hola de nuevo"
2. Make time handling deterministic in tests; do not rely on flaky wall-clock sleeps.
3. Preserve auditability by updating the Prisma backup on every state change that results from reconstruction or expiry reset.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest bot-conversation.service.spec --runInBand</automated>
  </verify>
  <done>
    - BOT-04 is proven under restart and Redis-loss scenarios
    - Returning-customer behavior follows the approved 24-hour rule
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add worker-level and e2e proof for multi-turn conversation continuity</name>
  <files>
    apps/backend/src/messaging/processors/message.processor.spec.ts
    apps/backend/test/bot-conversation.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/messaging/processors/message.processor.spec.ts
    apps/backend/test/webhooks.e2e-spec.ts
    .planning/phases/02-bot-conversation-engine/02-RESEARCH.md
  </read_first>
  <action>
1. Add worker/service tests that prove multiple inbound turns persist and reuse routing state correctly.
2. Add e2e proof that a lead can:
   - enter through greeting
   - progress into another state
   - continue after a simulated restart or Redis miss without being forced back to first-touch copy
3. Include at least one expiry-path test showing that an old conversation re-enters with the returning-customer greeting instead of a stale mid-flow state.
4. Keep the verification focused on Phase 2 routing guarantees; do not expand into unrelated CRM or quote-delivery coverage.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest message.processor.spec --runInBand</automated>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest bot-conversation.e2e-spec --runInBand</automated>
  </verify>
  <done>
    - Phase 2 claims are backed by repeatable tests instead of narrative assumptions
    - The worker boundary is proven, not just the service internals
  </done>
</task>

</tasks>

<verification>
1. `npx jest bot-conversation.service.spec --runInBand` passes
2. `npx jest message.processor.spec --runInBand` passes
3. `npx jest bot-conversation.e2e-spec --runInBand` passes
4. Active resume, Redis reconstruction, and 24-hour expiry behavior are all covered by tests
</verification>

<success_criteria>
- BOT-04 is satisfied with explicit recovery proof
- BOT-01 and BOT-03 are hardened by multi-turn and expiry-path verification
</success_criteria>
