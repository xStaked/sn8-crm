---
phase: 02-bot-conversation-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - apps/backend/src/bot-conversation/bot-conversation.service.ts
  - apps/backend/src/bot-conversation/intent-classifier.service.ts
  - apps/backend/src/bot-conversation/prompts/info-services.prompt.ts
  - apps/backend/src/bot-conversation/prompts/off-flow.prompt.ts
  - apps/backend/src/ai-sales/conversation-flow.service.ts
  - apps/backend/src/messaging/processors/message.processor.ts
  - apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
  - apps/backend/src/messaging/processors/message.processor.spec.ts
autonomous: true
requirements:
  - BOT-02
  - BOT-03

must_haves:
  truths:
    - "QUALIFYING state delegates to the existing ConversationFlowService instead of forking a second qualification engine"
    - "Free-text messages in GREETING can be classified into the approved intents with a safe bias toward quote qualification"
    - "Unexpected or off-flow messages receive bounded contextual guidance and increment a retry counter"
    - "After three consecutive off-flow attempts, the conversation escalates automatically to HUMAN_HANDOFF"
  artifacts:
    - path: "apps/backend/src/bot-conversation/intent-classifier.service.ts"
      provides: "Constrained intent classification contract for greeting free-text routing"
      contains: "GreetingIntent"
    - path: "apps/backend/src/bot-conversation/prompts/off-flow.prompt.ts"
      provides: "Prompt/builder or bounded fallback used for contextual redirect replies"
      contains: "OFF_FLOW"
    - path: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      provides: "QUALIFYING delegation and off-flow escalation behavior"
      contains: "offFlowCount"
  key_links:
    - from: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      to: "apps/backend/src/ai-sales/conversation-flow.service.ts"
      via: "QUALIFYING stage delegates to existing discovery logic"
      pattern: "planReply"
    - from: "apps/backend/src/bot-conversation/bot-conversation.service.ts"
      to: "apps/backend/src/messaging/processors/message.processor.ts"
      via: "worker uses routed decision and persists outbound response"
      pattern: "handleInbound"
---

<objective>
Implement qualification routing and resilient off-flow handling on top of the FSM foundation.

Purpose: let the bot guide leads through qualification without becoming brittle when users answer unexpectedly or send free text instead of pressing buttons.
Output: greeting intent classification, QUALIFYING delegation to the existing AI-sales discovery flow, contextual off-flow replies, media fallback, and automatic escalation after repeated failures.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bot-conversation-engine/02-CONTEXT.md
@.planning/phases/02-bot-conversation-engine/02-RESEARCH.md
@.planning/phases/02-bot-conversation-engine/02-01-PLAN.md
@.planning/phases/02-bot-conversation-engine/02-02-PLAN.md

<interfaces>
Current qualification engine:

From apps/backend/src/ai-sales/conversation-flow.service.ts:
```typescript
async planReply(input: {
  conversationId: string;
  inboundMessageId: string;
  inboundBody: string | null;
}): Promise<ReplyPlan>
```

Locked off-flow decisions from phase context:
- free-text in GREETING should be AI-classified into intent
- media gets "solo procesamos texto" and flow continues
- max 3 consecutive off-flow retries before human handoff
- retry counter resets after a valid answer

Normalized ingress dependency from Plan 02:
- the router can rely on explicit message kind and interactive reply metadata instead of inferring everything from `body`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add greeting intent classification and QUALIFYING delegation</name>
  <files>
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/intent-classifier.service.ts
    apps/backend/src/ai-sales/conversation-flow.service.ts
    apps/backend/src/bot-conversation/bot-conversation.service.spec.ts
  </files>
  <read_first>
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/ai-sales/conversation-flow.service.ts
    apps/backend/src/ai-sales/ai-provider.interface.ts
    .planning/phases/02-bot-conversation-engine/02-CONTEXT.md
  </read_first>
  <action>
1. Add a constrained greeting intent classifier for free-text messages that can resolve:
   - `quote_project`
   - `learn_services`
   - `human_handoff`
2. Bias ambiguity toward the quote path as specified in the context so the bot does not drop sales opportunities.
3. Route `quote_project` into `QUALIFYING`, then delegate message handling to the existing `ConversationFlowService.planReply(...)`.
4. Reset off-flow counters on valid routed input and persist the resulting state transitions.
5. Do not move quote generation or owner-review behavior back into Phase 2; QUALIFYING ends at the existing discovery/handoff boundary.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest bot-conversation.service.spec --runInBand</automated>
  </verify>
  <done>
    - BOT-02 is routed through the approved downstream qualification engine
    - Free-text greeting inputs no longer strand the lead outside the flow
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement off-flow, media fallback, and automatic escalation rules</name>
  <files>
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/bot-conversation/prompts/info-services.prompt.ts
    apps/backend/src/bot-conversation/prompts/off-flow.prompt.ts
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/messaging/processors/message.processor.spec.ts
  </files>
  <read_first>
    apps/backend/src/bot-conversation/bot-conversation.service.ts
    apps/backend/src/messaging/processors/message.processor.ts
    .planning/phases/02-bot-conversation-engine/02-CONTEXT.md
    .planning/phases/02-bot-conversation-engine/02-RESEARCH.md
  </read_first>
  <action>
1. Implement contextual off-flow replies that acknowledge what the lead said and guide them back to the current step.
2. Keep the behavior bounded and testable:
   - increment `offFlowCount` on invalid or unrelated input
   - reset it on valid input
   - escalate to HUMAN_HANDOFF automatically after the third consecutive off-flow turn
3. Add media fallback behavior keyed off the normalized message kind so non-text payloads respond with the approved "solo procesamos mensajes de texto" guidance instead of being silently ignored.
4. Update `MessageProcessor` guards only as needed so media messages can still reach the FSM fallback path while empty/owner-command protections remain intact.
5. Preserve INFO_SERVICES as an informational branch that can eventually route back into QUALIFYING when the lead is ready.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest message.processor.spec --runInBand</automated>
  </verify>
  <done>
    - BOT-03 is implemented with deterministic escalation rules
    - The bot can recover from messy real-world lead behavior
  </done>
</task>

</tasks>

<verification>
1. Greeting free-text intent classification reaches one of the approved states
2. QUALIFYING state still uses `ConversationFlowService.planReply(...)`
3. Off-flow retry counting, media fallback, and 3-strike escalation work without silent failure
</verification>

<success_criteria>
- BOT-02 is satisfied through routed qualification guidance
- BOT-03 is satisfied through bounded default handling and escalation
</success_criteria>
