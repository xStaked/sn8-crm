---
phase: 05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md
  - apps/backend/package.json
  - pnpm-lock.yaml
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/quote-documents/quote-pdf.service.ts
  - apps/backend/src/quote-documents/quote-pdf.service.spec.ts
  - apps/backend/src/quote-documents/quote-pdf.mapper.ts
  - apps/backend/src/quote-documents/documents/quote-pdf.document.tsx
  - apps/backend/src/quote-documents/quote-documents.module.ts
  - apps/backend/src/ai-sales/owner-review.service.ts
autonomous: true
requirements: [COT-04, QUOTE-PDF-02, QUOTE-PDF-03]

must_haves:
  truths:
    - "The commercial PDF layout is locked to an explicit contract before rendering code starts, so execution does not invent branding, legal text, or section structure ad hoc."
    - "The backend can render a branded commercial PDF for a specific `QuoteDraft.version` without requiring a browser runtime."
    - "A generated PDF artifact is cached per `QuoteDraft.id`, so regenerating a quote creates a new cache key instead of mutating or reusing the previous document."
    - "PDF generation remains a read-side document concern and does not create a new customer-delivery path outside `OwnerReviewService`."
  artifacts:
    - path: ".planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md"
      provides: "Locked commercial PDF template contract derived from the reference quote the team wants to emulate."
      contains: "## Locked Decisions"
    - path: "apps/backend/prisma/schema.prisma"
      provides: "Durable per-draft PDF artifact persistence keyed to `QuoteDraft`."
      contains: "model QuoteDraftDocument"
    - path: "apps/backend/src/quote-documents/quote-pdf.service.ts"
      provides: "Lazy generate-or-reuse PDF orchestration for the active draft."
      contains: "async getOrCreateDraftPdf"
    - path: "apps/backend/src/quote-documents/documents/quote-pdf.document.tsx"
      provides: "Commercial PDF layout rendered with `@react-pdf/renderer`."
      contains: "export function QuotePdfDocument"
  key_links:
    - from: "apps/backend/src/quote-documents/quote-pdf.service.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "QuoteDraftDocument lookup by quoteDraftId"
      pattern: "quoteDraftDocument"
    - from: "apps/backend/src/quote-documents/quote-pdf.service.ts"
      to: "apps/backend/src/quote-documents/documents/quote-pdf.document.tsx"
      via: "renderToBuffer or equivalent React-PDF render call"
      pattern: "QuotePdfDocument"
    - from: "apps/backend/src/quote-documents/quote-pdf.service.ts"
      to: "apps/backend/src/ai-sales/owner-review.service.ts"
      via: "shared latest-approved-or-reviewable draft boundary remains external to PDF service"
      pattern: "prepareApprovedCustomerDelivery|getLatestDraftForCommand|assertLatestDraftApproved"
---

<objective>
Establish the backend PDF foundation for commercial quote documents: install the renderer, persist one artifact per quote draft version, and expose a lazy generate-or-reuse service that can support CRM review without changing customer delivery behavior.

Purpose: make the quote lifecycle capable of producing a real commercial PDF artifact for the active draft while preserving the existing approval state machine.
Output: persistence model, PDF document renderer, quote PDF service, and backend tests for version-locked caching.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-02-SUMMARY.md
@Cotizacion - Norgtech.pdf

<interfaces>
From apps/backend/prisma/schema.prisma:
```prisma
model QuoteDraft {
  id                   String   @id @default(cuid())
  conversationId       String
  version              Int
  templateVersion      String
  draftPayload         Json
  renderedQuote        String?
  reviewStatus         QuoteReviewStatus
  approvedAt           DateTime?
  deliveredToCustomerAt DateTime?
}
```

From apps/backend/src/ai-sales/owner-review.service.ts:
```typescript
type ApprovedCustomerDeliveryPayload = {
  conversationId: string;
  quoteDraftId: string;
  version: number;
  body: string;
};

async prepareApprovedCustomerDelivery(conversationId: string): Promise<ApprovedCustomerDeliveryPayload>
```

From apps/backend/src/ai-sales/prompts/quote-draft.prompt.ts:
```typescript
"structuredDraft": {
  "ownerReviewDraft": {
    "title": string,
    "sections": Array<{ "label": string, "content": string }>,
    "pendingReviewLabel": string
  }
},
"renderedQuote": string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the commercial PDF template contract from the reference quote</name>
  <files>
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md
  </files>
  <read_first>
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md,
    Cotizacion - Norgtech.pdf,
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Before any rendering code, create a locked contract for what "formato comercial real" means in this repo.

1. Inspect `Cotizacion - Norgtech.pdf` as the source-of-truth reference artifact for layout/content expectations.
2. Create `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md` with these sections:
   - `## Locked Decisions`
   - `## Mandatory Sections`
   - `## Branding Rules`
   - `## Currency and Number Formatting`
   - `## Legal / Commercial Footer`
   - `## File Naming`
   - `## Executor Notes`
3. Lock, at minimum:
   - visible section order
   - header/title wording
   - customer/project identity fields
   - pricing / totals formatting rules
   - whether values are COP or another currency
   - footer/disclaimer text requirements
   - filename pattern for the generated PDF
4. If the reference PDF leaves any of those undefined, explicitly mark the fallback decision in `## Locked Decisions` so later tasks do not guess silently.
5. Treat this document as mandatory input for all later implementation tasks in this phase.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && test -f ".planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md` exists
    - `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md` contains `## Locked Decisions`
    - `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md` contains `## Mandatory Sections`
    - `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md` contains `## File Naming`
    - `test -f ".planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md"` exits 0
  </acceptance_criteria>
  <done>The phase now has an explicit PDF contract, so rendering work will implement a locked commercial template instead of inventing one.</done>
</task>

<task type="auto">
  <name>Task 2: Add PDF renderer dependency and durable per-draft document persistence</name>
  <files>
    apps/backend/package.json,
    pnpm-lock.yaml,
    apps/backend/prisma/schema.prisma
  </files>
  <read_first>
    apps/backend/package.json,
    apps/backend/prisma/schema.prisma,
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md,
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md,
    apps/backend/src/conversations/conversations.service.ts
  </read_first>
  <action>
Introduce the PDF rendering dependency and schema support needed for durable, version-locked quote documents.

1. Add `@react-pdf/renderer` to `apps/backend/package.json` and update `pnpm-lock.yaml`.
2. Extend `apps/backend/prisma/schema.prisma` with a `QuoteDraftDocument` model keyed by `quoteDraftId` and intended for the single v1 artifact kind `commercial_pdf`.
3. Include fields required by planning and verification: `fileName`, `mimeType`, `checksumSha256`, `sizeBytes`, `content`, `generatedAt`, and the relation back to `QuoteDraft`.
4. Preserve the existing `QuoteDraft` lifecycle and do not alter delivery-related fields or approval statuses in this task.
5. If the schema change requires relation fields on `QuoteDraft`, add them additively without renaming existing fields.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter backend exec prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/package.json` contains `@react-pdf/renderer`
    - `apps/backend/prisma/schema.prisma` contains `model QuoteDraftDocument`
    - `apps/backend/prisma/schema.prisma` contains `quoteDraftId`
    - `apps/backend/prisma/schema.prisma` contains `content       Bytes`
    - `pnpm --filter backend exec prisma validate` exits 0
  </acceptance_criteria>
  <done>The backend now has a supported PDF rendering dependency and a durable storage model for one commercial PDF artifact per quote draft version.</done>
</task>

<task type="auto">
  <name>Task 3: Implement lazy generate-or-reuse quote PDF service and commercial document layout</name>
  <files>
    apps/backend/src/quote-documents/quote-pdf.service.ts,
    apps/backend/src/quote-documents/quote-pdf.service.spec.ts,
    apps/backend/src/quote-documents/quote-pdf.mapper.ts,
    apps/backend/src/quote-documents/documents/quote-pdf.document.tsx,
    apps/backend/src/quote-documents/quote-documents.module.ts,
    apps/backend/src/ai-sales/owner-review.service.ts
  </files>
  <read_first>
    apps/backend/src/ai-sales/owner-review.service.ts,
    apps/backend/src/ai-sales/ai-sales.service.ts,
    apps/backend/src/ai-sales/ai-provider.interface.ts,
    apps/backend/src/ai-sales/prompts/quote-draft.prompt.ts,
    apps/backend/prisma/schema.prisma,
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-PDF-CONTRACT.md,
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md
  </read_first>
  <action>
Build the quote PDF foundation as a backend-only service that renders and caches a commercial document for a specific draft version.

1. Create `quote-pdf.mapper.ts` that derives a document view model from `QuoteDraft`, `CommercialBrief`, and the richest available structured quote data.
   - Prefer `draftPayload.structuredDraft.ownerReviewDraft.sections`
   - Fall back to `draftPayload.summary` and `renderedQuote`
2. Create `documents/quote-pdf.document.tsx` using `@react-pdf/renderer` primitives. It must follow `05.3.1-PDF-CONTRACT.md` and render:
   - quote title / version label
   - customer/project summary block
   - structured sections or fallback body
   - explicit internal-review status wording so the PDF is compatible with the approval flow
3. Create `quote-pdf.service.ts` with a method such as `getOrCreateDraftPdf(quoteDraftId: string)` or an equivalent conversation-aware variant that:
   - loads the target draft and required business context
   - returns an existing persisted document row if present
   - otherwise renders the PDF, calculates checksum/size, persists the row, and returns it
4. Register the service in `quote-documents.module.ts` and wire that module into the backend in a way future conversation endpoints can reuse.
5. Keep the approval boundary explicit:
   - Do not add any outbound customer send call to the PDF service
   - Do not alter `MessagingService.sendText()` semantics
   - If `OwnerReviewService` needs a tiny helper extraction to share draft lookup semantics, keep it additive and review-boundary-safe
6. Add focused specs proving:
   - first request renders and persists a document
   - second request for the same `quoteDraftId` reuses the cached artifact
   - a new regenerated draft/version does not reuse the previous draft’s document row
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/quote-documents/quote-pdf.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/quote-documents/quote-pdf.service.ts` contains `async getOrCreateDraftPdf`
    - `apps/backend/src/quote-documents/documents/quote-pdf.document.tsx` contains `QuotePdfDocument`
    - `apps/backend/src/quote-documents/quote-pdf.mapper.ts` reads `structuredDraft`
    - `apps/backend/src/quote-documents/quote-pdf.service.ts` writes `checksumSha256`
    - `apps/backend/src/quote-documents/quote-pdf.service.ts` writes `sizeBytes`
    - `apps/backend/src/quote-documents/quote-pdf.service.spec.ts` asserts cached reuse for the same draft
    - `npx jest apps/backend/src/quote-documents/quote-pdf.service.spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The backend can now lazily render and persist a commercial PDF artifact per draft version without introducing a second approval or delivery flow.</done>
</task>

</tasks>

<verification>
Run schema validation and the quote PDF service test suite:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter backend exec prisma validate && npx jest apps/backend/src/quote-documents/quote-pdf.service.spec.ts --runInBand`
</verification>

<success_criteria>
The codebase has a durable, version-locked PDF artifact model and a backend-only quote PDF service that renders a commercial document for the current draft without changing who controls customer delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-01-SUMMARY.md`.
</output>
