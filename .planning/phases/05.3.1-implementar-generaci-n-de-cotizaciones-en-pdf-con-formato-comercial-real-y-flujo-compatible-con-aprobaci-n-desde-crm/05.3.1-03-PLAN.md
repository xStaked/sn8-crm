---
phase: 05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm
plan: 03
type: execute
wave: 3
depends_on: [05.3.1-02]
files_modified:
  - apps/web/src/types/conversation.ts
  - apps/web/src/hooks/use-conversation-quote-review.ts
  - apps/web/src/components/shell/detail-panel.tsx
autonomous: false
requirements: [QUOTE-PDF-01]

must_haves:
  truths:
    - "The CRM detail panel exposes a clear action to open or download the commercial PDF for the currently selected quote draft."
    - "The PDF action stays inside the existing quote review card and does not replace or bypass approve/request-changes behavior."
    - "The UI state reflects PDF availability and the current draft version so the socio can verify the document before approving."
  artifacts:
    - path: "apps/web/src/types/conversation.ts"
      provides: "Typed PDF metadata on the quote review contract."
      contains: "pdf:"
    - path: "apps/web/src/hooks/use-conversation-quote-review.ts"
      provides: "Frontend type/fetch support for quote review responses with PDF metadata."
      contains: "ConversationQuoteReviewDto"
    - path: "apps/web/src/components/shell/detail-panel.tsx"
      provides: "Open/download PDF affordance inside the quote review card."
      contains: "Abrir PDF"
  key_links:
    - from: "apps/web/src/components/shell/detail-panel.tsx"
      to: "/conversations/:conversationId/quote-review/pdf"
      via: "window open or download link from the selected conversation"
      pattern: "quote-review/pdf"
    - from: "apps/web/src/components/shell/detail-panel.tsx"
      to: "apps/web/src/hooks/use-conversation-quote-review.ts"
      via: "quoteReview.pdf availability state"
      pattern: "quoteReview\\.pdf"
---

<objective>
Integrate the commercial quote PDF into the existing CRM quote-review card so the socio can open or download the exact document they are about to approve, without changing the surrounding review flow.

Purpose: make the PDF operational in the real CRM workflow rather than leaving it as a backend-only capability.
Output: typed frontend contract updates plus a quote-review card action for opening/downloading the PDF.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md
@.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-02-PLAN.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-03-SUMMARY.md

<interfaces>
From apps/web/src/hooks/use-conversation-quote-review.ts:
```typescript
export function useConversationQuoteReview(conversationId: string | null) {
  // returns quoteReview, state, mutate
}
```

From apps/web/src/components/shell/detail-panel.tsx:
```tsx
// Existing quote review card above the message history and compose area
// Existing actions:
// - approve
// - request changes
```

Planned backend route from 05.3.1-02:
```text
GET /conversations/:conversationId/quote-review/pdf
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend quote review types and hook with PDF metadata</name>
  <files>
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversation-quote-review.ts
  </files>
  <read_first>
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversation-quote-review.ts,
    apps/web/src/lib/api.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts
  </read_first>
  <action>
Update the frontend quote review contract to understand the additive PDF metadata coming from the backend.

1. Extend the relevant quote review type(s) in `apps/web/src/types/conversation.ts` with a `pdf` object containing at least:
   - `available`
   - `fileName`
   - `generatedAt`
   - `sizeBytes`
   - `version`
2. Keep the current hook API stable; `useConversationQuoteReview()` should continue returning the same high-level shape with the richer typed payload.
3. Preserve existing 401 redirect handling and 404-to-empty-state behavior.
4. Do not create a second fetch hook just for the PDF if a direct URL can be opened from the review card using the authenticated browser session.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/types/conversation.ts` contains `pdf`
    - `apps/web/src/types/conversation.ts` contains `sizeBytes`
    - `apps/web/src/hooks/use-conversation-quote-review.ts` still exports `useConversationQuoteReview`
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The frontend can now represent PDF availability and metadata for the quote under review without changing the existing hook contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add open/download PDF affordance to the CRM quote review card</name>
  <files>
    apps/web/src/components/shell/detail-panel.tsx
  </files>
  <read_first>
    apps/web/src/components/shell/detail-panel.tsx,
    apps/web/src/hooks/use-conversation-quote-review.ts,
    apps/web/src/types/conversation.ts,
    .planning/phases/05.3-quote-approval-from-crm/05.3-03-SUMMARY.md
  </read_first>
  <action>
Integrate the new PDF capability into the existing quote review card without disrupting the approval workflow or shell layout.

1. Add a compact PDF action area in the quote review card that shows:
   - document availability
   - current draft version
   - a primary or secondary action labeled `Abrir PDF` or `Descargar PDF`
2. Build the URL from the existing selected conversation id using `/conversations/:conversationId/quote-review/pdf`.
3. Use the current authenticated browser session for opening/downloading the PDF. Prefer a normal anchor/button opening a new tab over a custom blob-fetch flow unless the existing auth model makes that impossible.
4. Keep approve and request-changes actions intact and visually subordinate only when the PDF is unavailable; do not disable approval just because the PDF is not yet cached if the endpoint can generate it lazily on open.
5. Surface light metadata such as generated timestamp or file size when available, but do not turn this into a new standalone page or modal.
6. Preserve the existing layout patterns and error states from Phase 05.3; this must remain an additive enhancement to the same detail panel card.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/shell/detail-panel.tsx` contains `Abrir PDF` or `Descargar PDF`
    - `apps/web/src/components/shell/detail-panel.tsx` contains `quote-review/pdf`
    - `apps/web/src/components/shell/detail-panel.tsx` references `quoteReview.pdf`
    - `apps/web/src/components/shell/detail-panel.tsx` still contains approve and request-changes handlers
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The CRM detail panel now gives the socio a direct path to the commercial PDF for the current quote draft while preserving the established approval/revision workflow.</done>
</task>

</tasks>

<verification>
Run frontend type-checking after the quote review card integration:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit`
</verification>

<success_criteria>
Inside the existing CRM quote review card, the socio can open or download the version-correct commercial PDF for the active draft without navigating away or changing how approval works.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-03-SUMMARY.md`.
</output>
