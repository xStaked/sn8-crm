---
phase: 05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm
plan: 02
type: execute
wave: 2
depends_on: [05.3.1-01]
files_modified:
  - apps/backend/src/conversations/dto/quote-review-response.dto.ts
  - apps/backend/src/conversations/conversations.service.ts
  - apps/backend/src/conversations/conversations.controller.ts
  - apps/backend/src/conversations/conversations.service.spec.ts
  - apps/backend/src/conversations/conversations.controller.spec.ts
  - apps/backend/test/conversations.e2e-spec.ts
autonomous: true
requirements: [COT-04, QUOTE-PDF-01, QUOTE-PDF-02, QUOTE-PDF-03]

must_haves:
  truths:
    - "The CRM can retrieve metadata for the PDF tied to the latest review-relevant draft and download the actual `application/pdf` payload through authenticated conversation-scoped routes."
    - "The PDF endpoint returns the document for the same draft version the CRM review card is showing, not a stale artifact from an older quote version."
    - "The new read/download endpoints do not send anything to the customer and remain additive to the existing quote-review contract."
  artifacts:
    - path: "apps/backend/src/conversations/dto/quote-review-response.dto.ts"
      provides: "Additive PDF metadata on the existing quote review DTO."
      contains: "pdf:"
    - path: "apps/backend/src/conversations/conversations.service.ts"
      provides: "Conversation-scoped access to quote PDF metadata and bytes."
      contains: "getConversationQuoteReviewPdf"
    - path: "apps/backend/src/conversations/conversations.controller.ts"
      provides: "Authenticated quote PDF download endpoint."
      contains: "@Get('conversations/:conversationId/quote-review/pdf')"
  key_links:
    - from: "apps/backend/src/conversations/conversations.service.ts"
      to: "apps/backend/src/quote-documents/quote-pdf.service.ts"
      via: "generate-or-reuse PDF for the latest review-relevant draft"
      pattern: "quotePdfService"
    - from: "apps/backend/src/conversations/conversations.controller.ts"
      to: "apps/backend/src/conversations/conversations.service.ts"
      via: "getConversationQuoteReviewPdf(conversationId)"
      pattern: "this\\.conversationsService\\.getConversationQuoteReviewPdf"
    - from: "apps/backend/test/conversations.e2e-spec.ts"
      to: "apps/backend/src/conversations/conversations.controller.ts"
      via: "authenticated GET pdf route contract"
      pattern: "quote-review/pdf"
---

<objective>
Expose the generated quote PDF to the CRM through the existing conversations surface, with additive metadata on the review DTO and an authenticated endpoint that streams the version-locked PDF bytes.

Purpose: let the CRM access the real commercial document that the socio is reviewing, without introducing a new top-level quote module or touching customer delivery.
Output: PDF metadata in quote-review reads, a conversation-scoped PDF route, and automated contract coverage.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md
@.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-01-PLAN.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-01-SUMMARY.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-02-SUMMARY.md

<interfaces>
From apps/backend/src/conversations/dto/quote-review-response.dto.ts:
```typescript
export class ConversationQuoteReviewDto {
  conversationId: string;
  quoteDraftId: string;
  version: number;
  reviewStatus: string;
  renderedQuote: string | null;
  deliveredToCustomerAt: string | null;
}
```

From apps/backend/src/conversations/conversations.service.ts:
```typescript
async getConversationQuoteReview(conversationId: string): Promise<ConversationQuoteReviewDto>
```

Planned from 05.3.1-01:
```typescript
async getOrCreateDraftPdf(quoteDraftId: string): Promise<{
  fileName: string;
  mimeType: string;
  sizeBytes: number;
  generatedAt: Date;
  content: Buffer;
}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend quote-review DTOs and conversation service with PDF metadata/read access</name>
  <files>
    apps/backend/src/conversations/dto/quote-review-response.dto.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/conversations/conversations.service.spec.ts
  </files>
  <read_first>
    apps/backend/src/conversations/dto/quote-review-response.dto.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/quote-documents/quote-pdf.service.ts,
    apps/backend/src/conversations/conversations.service.spec.ts,
    .planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-RESEARCH.md
  </read_first>
  <action>
Add additive PDF metadata to the existing quote review read model and expose a service method that resolves bytes for the current reviewable draft.

1. Extend `ConversationQuoteReviewDto` with a `pdf` field such as:
   - `available`
   - `fileName`
   - `generatedAt`
   - `sizeBytes`
   - `version`
2. Update `getConversationQuoteReview()` so it reports PDF metadata when the stored artifact already exists, without forcing generation just to render the detail card.
3. Add `getConversationQuoteReviewPdf(conversationId: string)` that:
   - resolves the latest review-relevant draft for the normalized conversation id
   - calls the quote PDF service to generate or reuse the artifact for that exact draft
   - returns enough data for the controller to stream `application/pdf`
4. Keep the service additive: no mutation of review status, no delivery side effects, and no change to the existing `pendingQuote` or approval action methods.
5. Extend the service spec to prove:
   - metadata is included when a document row exists
   - missing metadata remains `null` or `available: false` without erroring
   - the PDF fetch path targets the latest review-relevant draft version
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/conversations/dto/quote-review-response.dto.ts` contains `pdf`
    - `apps/backend/src/conversations/conversations.service.ts` contains `async getConversationQuoteReviewPdf`
    - `apps/backend/src/conversations/conversations.service.ts` references `quotePdfService`
    - `apps/backend/src/conversations/conversations.service.spec.ts` asserts the latest draft version is selected for PDF access
    - `npx jest apps/backend/src/conversations/conversations.service.spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The conversations service can now describe and retrieve the current commercial PDF artifact for the quote under review without disturbing approval state.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated PDF download endpoint and end-to-end contract coverage</name>
  <files>
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts
  </read_first>
  <action>
Expose a conversation-scoped PDF route that the CRM can open in a new tab or download directly.

1. Add `GET /conversations/:conversationId/quote-review/pdf` behind `JwtAuthGuard`.
2. Stream the PDF bytes with:
   - `Content-Type: application/pdf`
   - a deterministic `Content-Disposition` filename tied to the draft/version
3. Keep the existing quote-review JSON route intact and document the additive PDF metadata in Swagger or DTO decorators as appropriate for the current controller style.
4. Extend controller specs to verify:
   - service delegation to `getConversationQuoteReviewPdf`
   - correct headers for PDF responses
5. Extend `apps/backend/test/conversations.e2e-spec.ts` to verify:
   - authenticated users can call the pdf route
   - the route returns `application/pdf`
   - the returned filename/version matches the active draft, not a stale one
   - no approval status changes occur as a side effect of reading the PDF
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/conversations/conversations.controller.ts` contains `@Get('conversations/:conversationId/quote-review/pdf')`
    - `apps/backend/src/conversations/conversations.controller.ts` contains `application/pdf`
    - `apps/backend/src/conversations/conversations.controller.spec.ts` asserts `getConversationQuoteReviewPdf`
    - `apps/backend/test/conversations.e2e-spec.ts` contains `quote-review/pdf`
    - `apps/backend/test/conversations.e2e-spec.ts` asserts `content-type`
    - `npx jest apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The backend now exposes a safe, authenticated conversation-scoped PDF download path that always corresponds to the current draft version under review.</done>
</task>

</tasks>

<verification>
Run the conversation service, controller, and e2e contract tests for the additive PDF routes:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.service.spec.ts apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand`
</verification>

<success_criteria>
The CRM-facing backend can describe whether a commercial quote PDF exists and can stream the correct version-locked `application/pdf` artifact through authenticated conversation routes.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3.1-implementar-generaci-n-de-cotizaciones-en-pdf-con-formato-comercial-real-y-flujo-compatible-con-aprobaci-n-desde-crm/05.3.1-02-SUMMARY.md`.
</output>
