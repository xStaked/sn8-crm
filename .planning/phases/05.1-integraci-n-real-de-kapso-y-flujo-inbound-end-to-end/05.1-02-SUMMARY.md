---
phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end
plan: 02
subsystem: api
tags: [kapso, prisma, conversations, nestjs, jest, bullmq]
requires:
  - phase: 05.1-01
    provides: "Nested Kapso ingress normalization and webhook id extraction for realistic inbound payloads"
  - phase: 05-frontend-integration-with-current-backend
    provides: "Existing CRM conversation list/detail endpoints and frontend contract"
provides:
  - "Inbound message persistence now preserves the original payload even when normalized rawPayload is absent"
  - "Processor coverage proving externalMessageId-backed durable idempotency for repeated inbound deliveries"
  - "Endpoint-level proof that a persisted inbound message reappears in /conversations and /conversations/:conversationId/messages with the same stable phone-derived id"
affects: [05.1-03, conversations, inbound-persistence, crm-read-model]
tech-stack:
  added: []
  patterns: ["Message durability is enforced by Prisma uniqueness on externalMessageId plus processor-level duplicate success handling", "Conversation read proof should reuse persisted Message rows instead of introducing a parallel projection path"]
key-files:
  created: [".planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-02-SUMMARY.md"]
  modified:
    [
      "apps/backend/src/messaging/processors/message.processor.ts",
      "apps/backend/src/messaging/processors/message.processor.spec.ts",
      "apps/backend/src/conversations/conversations.service.spec.ts",
      "apps/backend/test/conversations.e2e-spec.ts"
    ]
key-decisions:
  - "When normalized inbound payloads omit rawPayload, persistence falls back to the original webhook payload so troubleshooting data is never lost."
  - "Read-model proof reuses MessageProcessor-backed durable rows in e2e tests instead of adding a second messaging test harness."
patterns-established:
  - "Inbound persistence tests should assert both duplicate P2002 success and durable single-row outcomes for the same externalMessageId."
  - "Conversation endpoint verification should assert the same phone-derived conversationId across list and history responses after persistence."
requirements-completed: [KAPSO-E2E-02, KAPSO-E2E-03]
duration: 4min
completed: 2026-03-18
---

# Phase 05.1 Plan 02: Durable inbound persistence and conversation read-model proof Summary

**Inbound Kapso messages now persist idempotently with retained raw payloads and reappear in both CRM conversation endpoints under one stable phone-derived conversation id.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-19T00:47:45Z
- **Completed:** 2026-03-19T00:51:51Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Locked `MessageProcessor` coverage around `externalMessageId`, duplicate `P2002` success, and durable single-row outcomes for repeated inbound deliveries.
- Closed a persistence gap so the original inbound payload is stored even if the channel adapter omits `rawPayload`.
- Added service and e2e proof that one persisted inbound message updates both `/conversations` and `/conversations/:conversationId/messages` without contract drift.

## Task Commits

Each task was committed atomically:

1. **Task 1: Lock idempotent inbound persistence around externalMessageId** - `eff584e` (fix)
2. **Task 2: Prove the persisted inbound message updates the conversation read APIs** - `ba85372` (test)

## Files Created/Modified

- `apps/backend/src/messaging/processors/message.processor.ts` - Falls back to the original webhook payload when normalized raw payload data is missing.
- `apps/backend/src/messaging/processors/message.processor.spec.ts` - Proves durable idempotency, duplicate success handling, and troubleshooting payload retention.
- `apps/backend/src/conversations/conversations.service.spec.ts` - Verifies summary and history endpoints share the same stable phone-derived conversation id.
- `apps/backend/test/conversations.e2e-spec.ts` - Exercises persisted inbound deliveries through `MessageProcessor` into both conversation endpoints and duplicate-delivery reads.

## Decisions Made

- Preserved the current schema and conversation projection strategy; the proof layer was added entirely through processor/service/e2e coverage.
- Reused the real `MessageProcessor` in e2e coverage so duplicate-delivery assertions are tied to the same persistence behavior the application runs in production.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Persist the original inbound payload when normalized raw data is absent**
- **Found during:** Task 1 (Lock idempotent inbound persistence around externalMessageId)
- **Issue:** A normalized inbound message without `rawPayload` would persist `undefined`, removing troubleshooting evidence from the durable `Message` row.
- **Fix:** `MessageProcessor` now falls back to the original job payload before writing `rawPayload`.
- **Files modified:** `apps/backend/src/messaging/processors/message.processor.ts`, `apps/backend/src/messaging/processors/message.processor.spec.ts`
- **Verification:** `npx jest message.processor.spec --runInBand`
- **Committed in:** `eff584e`

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Necessary for durable troubleshooting evidence. No schema or architecture change was introduced.

## Issues Encountered

- `apps/backend/prisma/schema.prisma` did not exist at the path referenced in the plan read-first list; the active Prisma schema is `prisma/schema.prisma`, and it matched the existing uniqueness strategy on `Message.externalMessageId`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The backend now has executable proof that inbound Kapso messages survive persistence and drive the current CRM read endpoints without duplicate durable state.
- Phase 05.1-03 can focus on local/manual end-to-end verification and any minimal frontend refresh behavior needed to surface the persisted data live.

## Self-Check: PASSED

- Confirmed `.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-02-SUMMARY.md` exists.
- Confirmed task commits `eff584e` and `ba85372` exist in git history.

---
*Phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end*
*Completed: 2026-03-18*
