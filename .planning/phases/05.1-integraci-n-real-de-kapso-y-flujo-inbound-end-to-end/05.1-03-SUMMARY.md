---
phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end
plan: 03
subsystem: testing
tags: [kapso, nextjs, swr, conversations, runbook, e2e]
requires:
  - phase: 05.1-02
    provides: "Durable inbound persistence and conversation read-model proof backed by persisted Message rows"
  - phase: 05-frontend-integration-with-current-backend
    provides: "Current CRM inbox/detail shell already connected to the real backend contracts"
provides:
  - "A repeatable local runbook for signed Kapso webhook smoke tests and optional real-tunnel delivery"
  - "Verified end-to-end evidence that one inbound Kapso message appears exactly once in webhook handling, conversation APIs, and the current CRM inbox/detail"
  - "Minimal SWR refresh glue so the existing CRM shell reveals inbound updates without changing conversation contracts or redesigning UI"
affects: [phase-05-verification, kapso-e2e, crm-shell, local-smoke]
tech-stack:
  added: []
  patterns: ["End-to-end Kapso verification should prove webhook acceptance, duplicate suppression, API visibility, and CRM visibility from one documented smoke run", "Frontend freshness for this CRM shell stays in the hooks layer via low-frequency SWR revalidation instead of UI rewrites"]
key-files:
  created: [".planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-03-SUMMARY.md"]
  modified:
    [
      ".env.example",
      ".planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md",
      "apps/web/src/hooks/use-conversations.ts",
      "apps/web/src/hooks/use-conversation-messages.ts"
    ]
key-decisions:
  - "The inbound smoke proof is satisfied by one documented signed local webhook flow plus optional real Kapso tunnel delivery; no extra backend scripts were introduced."
  - "Frontend visibility changes stayed inside the existing SWR hooks with a 5-second refresh interval so inbox/detail data updates without changing API contracts or redesigning components."
patterns-established:
  - "Local Kapso verification should start from seeded auth, then reuse the same login cookie jar for API assertions and browser confirmation."
  - "When frontend/backend live in separate repos, phase summaries must record task commits across both repos so checkpoint evidence remains auditable."
requirements-completed: [KAPSO-E2E-04, KAPSO-E2E-05]
duration: 19 min
completed: 2026-03-18
---

# Phase 05.1 Plan 03: Local Kapso smoke proof and CRM visibility Summary

**Signed local Kapso inbound smoke coverage is now documented end-to-end, and the current CRM shell visibly refreshes the persisted inbound message in inbox and detail without contract drift.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-03-19T00:56:22Z
- **Completed:** 2026-03-19T01:15:03Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Documented a reproducible local smoke path covering signed webhook delivery, duplicate replay, authenticated API checks, and CRM UI verification.
- Confirmed with local runtime evidence that `POST /webhooks/kapso` persisted `wamid.local-smoke-20260318` exactly once and surfaced the same data through `/conversations` and `/conversations/:conversationId/messages`.
- Kept the frontend change minimal by relying on low-frequency SWR refresh in the existing inbox/detail hooks so the dashboard reveals inbound updates without a redesign.

## Task Commits

Each task was committed atomically:

1. **Task 1: Document local setup and signed inbound smoke flow** - `80abfab` (feat, parent repo)
2. **Task 2: Make the current CRM shell surface the inbound update with minimal change** - `aab5c79` (feat, `apps/web` repo)

## Files Created/Modified

- `.env.example` - Exposes the environment variables required for local Kapso webhook verification and frontend/backend wiring.
- `.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md` - Captures the signed local webhook smoke runbook and the optional real Kapso tunnel flow.
- `apps/web/src/hooks/use-conversations.ts` - Polls the existing conversation summary endpoint so inbox previews surface inbound updates.
- `apps/web/src/hooks/use-conversation-messages.ts` - Polls the existing conversation history endpoint so the detail panel reflects persisted inbound history.

## Decisions Made

- Treated the user-provided backend and frontend verification evidence as the completion signal for the final verification task instead of rerunning the full smoke flow.
- Recorded Task 2 against the nested `apps/web` repository because that frontend lives under its own git history and the commit already existed there.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The checkpoint resume table initially looked inconsistent because the parent repo did not contain `aab5c79`; verification showed that `apps/web` is a nested git repository and the hook commit existed there exactly as referenced.

## User Setup Required

None - no new external configuration beyond the documented local env values and optional tunnel workflow.

## Next Phase Readiness

- Phase 05.1 is now complete with explicit local evidence that Kapso inbound data survives webhook validation, duplicate suppression, persistence, conversation reads, and CRM rendering.
- The remaining frontend verification gap lives in Phase 05 (`FE-BE-04`), which can now reuse the same runtime environment and inbox/detail proof path.

## Self-Check: PASSED

- Confirmed `.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-03-SUMMARY.md` exists.
- Confirmed parent-repo task commit `80abfab` exists in git history.
- Confirmed nested `apps/web` task commit `aab5c79` exists in git history.

---
*Phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end*
*Completed: 2026-03-18*
