---
phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end
plan: 02
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - apps/backend/src/messaging/processors/message.processor.ts
  - apps/backend/src/messaging/processors/message.processor.spec.ts
  - apps/backend/src/conversations/conversations.service.ts
  - apps/backend/src/conversations/conversations.service.spec.ts
  - apps/backend/test/conversations.e2e-spec.ts
autonomous: true
requirements:
  - KAPSO-E2E-02
  - KAPSO-E2E-03

must_haves:
  truths:
    - "A repeated inbound delivery produces at most one durable Message row for a given externalMessageId"
    - "The same phone-based stable conversation id is used when the persisted inbound message is read back through the CRM endpoints"
    - "The read endpoints the frontend already consumes expose the newly persisted inbound message without contract drift"
    - "The current schema and projection strategy remain sufficient; no second messaging architecture is introduced"
  artifacts:
    - path: "apps/backend/src/messaging/processors/message.processor.ts"
      provides: "Durable idempotent persistence of normalized inbound messages"
      contains: "externalMessageId"
    - path: "apps/backend/src/conversations/conversations.service.ts"
      provides: "Stable conversation read model over persisted inbound/outbound messages"
      contains: "getStableConversationId"
    - path: "apps/backend/test/conversations.e2e-spec.ts"
      provides: "Contract proof that a persisted inbound message appears in both conversation list and history endpoints"
      contains: "/conversations"
  key_links:
    - from: "apps/backend/src/messaging/processors/message.processor.ts"
      to: "apps/backend/src/conversations/conversations.service.ts"
      via: "persisted Message rows are the only input to the read model"
      pattern: "prisma\\.message"
    - from: "apps/backend/test/conversations.e2e-spec.ts"
      to: "apps/backend/src/conversations/conversations.service.ts"
      via: "same stable phone-derived conversation id must be visible in API output"
      pattern: "conversationId"
---

<objective>
Prove that a real inbound Kapso message becomes durable state and then reappears in the CRM read endpoints without duplication.

Purpose: turn the ingress work into business-visible evidence by following the message into `Message`, `/conversations`, and `/conversations/:conversationId/messages`.
Output: idempotent persistence plus endpoint-level proof that the stored inbound message updates the current conversation projections.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-CONTEXT.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-01-SUMMARY.md
@.planning/phases/05-frontend-integration-with-current-backend/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock idempotent inbound persistence around externalMessageId</name>
  <files>
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/messaging/processors/message.processor.spec.ts
  </files>
  <read_first>
    apps/backend/src/messaging/processors/message.processor.ts
    apps/backend/src/messaging/processors/message.processor.spec.ts
    prisma/schema.prisma
  </read_first>
  <action>
1. Review the current message processor against the phase target and keep the schema unchanged unless a real blocker is discovered.
2. Expand processor tests so they explicitly prove:
   - a normalized inbound message is persisted with the expected `externalMessageId`
   - duplicate persistence is treated as successful idempotency
   - repeated deliveries do not create a second durable message row
   - raw payload remains available for troubleshooting
3. If an implementation gap is found while writing the tests, fix it in the processor without changing the messaging architecture.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest message.processor.spec --runInBand</automated>
  </verify>
  <done>
    - Inbound persistence is explicitly locked to `externalMessageId`
    - Duplicate deliveries do not turn into duplicate `Message` rows
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the persisted inbound message updates the conversation read APIs</name>
  <files>
    apps/backend/src/conversations/conversations.service.ts
    apps/backend/src/conversations/conversations.service.spec.ts
    apps/backend/test/conversations.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/conversations/conversations.service.ts
    apps/backend/src/conversations/conversations.service.spec.ts
    apps/backend/test/conversations.e2e-spec.ts
    apps/backend/src/webhooks/webhooks.service.ts
  </read_first>
  <action>
1. Extend the conversations specs/e2e coverage so one persisted inbound Kapso message can be traced through:
   - `GET /conversations`
   - `GET /conversations/:conversationId/messages`
2. Ensure the assertions prove the same stable phone-derived `conversationId` is used in both endpoints.
3. Add a duplicate-delivery scenario that confirms repeating the same inbound event does not change the durable row count while the read model still shows the single expected message.
4. Preserve the existing inbox/detail response contract; only change endpoint behavior if a real inconsistency with the persisted data is discovered.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest conversations.service.spec --runInBand</automated>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest test/conversations.e2e-spec.ts --runInBand</automated>
  </verify>
  <done>
    - The read APIs expose the inbound message created by the webhook/persistence path
    - The phone-based conversation identity remains stable
    - The backend contract consumed by the CRM stays intact
  </done>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx jest message.processor.spec --runInBand` passes
2. `cd apps/backend && npx jest conversations.service.spec --runInBand` passes
3. `cd apps/backend && npx jest test/conversations.e2e-spec.ts --runInBand` passes
4. Repeating the same inbound event does not create duplicate `Message` state
5. The stored inbound message appears in both current conversation endpoints
</verification>

<success_criteria>
- KAPSO-E2E-02: inbound persistence is durable and idempotent
- KAPSO-E2E-03: the same phone-derived conversation id connects persistence to the existing read APIs
</success_criteria>
