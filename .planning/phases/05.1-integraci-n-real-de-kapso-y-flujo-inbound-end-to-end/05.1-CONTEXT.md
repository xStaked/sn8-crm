# Phase 05.1: Integración real de Kapso y flujo inbound end-to-end - Context

**Gathered:** 2026-03-18
**Status:** Ready for planning
**Source:** User planning brief + current codebase review

<domain>
## Phase Boundary

Esta fase no crea un CRM nuevo ni rediseña el frontend. La meta es cerrar el tramo faltante entre el webhook inbound de Kapso y las vistas CRM que ya consumen `GET /conversations` y `GET /conversations/:conversationId/messages`.

El entregable real es este recorrido único y verificable:

1. Kapso envía un webhook inbound real o simulado al backend actual.
2. El backend valida firma/secret.
3. El payload se normaliza a un formato interno consistente.
4. El mensaje se persiste en `Message` sin duplicarse por `externalMessageId`.
5. La conversación se resuelve con la misma regla estable basada en identidad telefónica.
6. El mensaje reaparece en las APIs de conversaciones.
7. El CRM frontend ya integrado refleja ese mensaje persistido.

La fase debe mantener foco en "mensaje entrante real entra por webhook, se persiste y aparece en CRM".
</domain>

<decisions>
## Implementation Decisions

### Locked Decisions
- Reusar el endpoint actual `POST /webhooks/kapso`; no introducir una arquitectura de mensajería nueva si la actual soporta el flujo.
- Mantener la validación de firma o secret del webhook como requisito duro; un request inválido debe rechazarse.
- Mantener la persistencia en la tabla `Message` con idempotencia por `externalMessageId`.
- Mantener la regla estable de `conversationId` basada en phone identity; no cambiar el contrato actual de inbox/detail salvo necesidad estricta.
- Mantener el frontend actual sin rediseño. Solo se permiten ajustes mínimos para que el dato persistido se refleje correctamente.
- Priorizar primero backend webhook/persistencia y después verificación end-to-end.
- La fase debe dejar evidencia ejecutable hoy localmente, con variables de entorno reales o dummy controladas.

### Claude's Discretion
- Aceptar una o más variantes del payload inbound real de Kapso siempre que converjan al mismo `NormalizedMessage`.
- Elegir si la prueba end-to-end usa delivery simulado firmado, túnel local con Kapso real, o ambos, siempre que quede documentado.
- Añadir un ajuste mínimo de revalidación/refresco en frontend solo si la evidencia muestra que el mensaje persistido no reaparece con la UX actual.
</decisions>

<specifics>
## Specific Ideas

- Variables existentes ya disponibles:
  - `KAPSO_API_KEY`
  - `KAPSO_PHONE_NUMBER_ID`
  - `KAPSO_WEBHOOK_SECRET`
- Endpoints ya existentes:
  - `GET /conversations`
  - `GET /conversations/:conversationId/messages`
- Stack ya existente:
  - NestJS
  - Prisma
  - Redis
  - JWT por cookie
- Evidencia esperada:
  - mensaje inbound aparece en `GET /conversations`
  - mensaje inbound aparece en `GET /conversations/:conversationId/messages`
  - inbox y detail panel del CRM muestran el dato persistido

Observación de código relevante: el webhook, la firma HMAC y la cola ya existen desde Phase 1, pero el extractor de idempotency key y la cobertura actual todavía están centrados en un payload simplificado (`payload.message.id`). La fase debe cerrar esa brecha para payload real de Kapso/Meta y conectarla a la evidencia de lectura del CRM.
</specifics>

<deferred>
## Deferred Ideas

- Rediseño visual del CRM
- Nueva arquitectura de eventos o mensajería
- Tiempo real full push/WebSocket
- Gestión avanzada de contactos o unread state real
</deferred>

---

*Phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end*
*Context gathered: 2026-03-18 via user brief + code review*
