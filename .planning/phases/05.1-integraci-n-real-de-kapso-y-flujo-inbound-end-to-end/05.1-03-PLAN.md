---
phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - 05.1-01
  - 05.1-02
files_modified:
  - .env.example
  - apps/backend/package.json
  - apps/web/src/hooks/use-conversations.ts
  - apps/web/src/hooks/use-conversation-messages.ts
  - .planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md
autonomous: false
requirements:
  - KAPSO-E2E-04
  - KAPSO-E2E-05

must_haves:
  truths:
    - "There is a documented local path that proves webhook -> persistence -> APIs -> CRM today"
    - "The current inbox and detail panel reveal the persisted inbound message without redesigning the frontend"
    - "Any frontend change in this phase is minimal and only serves data visibility or refresh, not a UI rewrite"
    - "The smoke flow can be run with either real Kapso credentials or dummy-controlled signed payloads"
  artifacts:
    - path: ".planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md"
      provides: "Repeatable setup and smoke runbook for local Kapso inbound verification"
      contains: "/webhooks/kapso"
    - path: "apps/web/src/hooks/use-conversations.ts"
      provides: "Minimal inbox refresh behavior for revealing persisted inbound updates when needed"
      contains: "useSWR"
    - path: "apps/web/src/hooks/use-conversation-messages.ts"
      provides: "Minimal detail refresh behavior for revealing persisted inbound history when needed"
      contains: "useSWR"
  key_links:
    - from: ".planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md"
      to: "apps/backend/package.json"
      via: "documented commands must match the repo's actual scripts"
      pattern: "seed:user"
    - from: "apps/web/src/hooks/use-conversations.ts"
      to: "apps/backend/src/conversations/conversations.controller.ts"
      via: "frontend still consumes the same existing list contract"
      pattern: "/conversations"
---

<objective>
Package the inbound Kapso integration into a reproducible local runbook and prove it is visible in the current CRM shell.

Purpose: finish the phase with explicit end-to-end evidence instead of isolated backend confidence.
Output: setup documentation, a smoke path for signed local or real Kapso delivery, and only the smallest frontend refresh glue needed to surface the persisted message in inbox/detail.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-CONTEXT.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-03-SUMMARY.md
@.planning/phases/05-frontend-integration-with-current-backend/05-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document local setup and signed inbound smoke flow</name>
  <files>
    .env.example
    apps/backend/package.json
    .planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-LOCAL-SETUP.md
  </files>
  <read_first>
    .env.example
    apps/backend/package.json
    .planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-RESEARCH.md
  </read_first>
  <action>
1. Confirm `.env.example` exposes every variable the runbook needs for local Kapso inbound testing.
2. Create `05.1-LOCAL-SETUP.md` with two explicit flows:
   - controlled local simulation using a signed JSON payload against `POST /webhooks/kapso`
   - optional real Kapso delivery via local tunnel
3. The runbook must include:
   - backend/frontend startup order
   - required env vars
   - a repeatable signed webhook example
   - how to verify the result in `GET /conversations`
   - how to verify the result in `GET /conversations/:conversationId/messages`
   - how to verify the same result inside the CRM dashboard
4. Reuse the existing `seed:user` backend script if authentication setup is part of the smoke flow. Do not add redundant scripts unless a missing script blocks the runbook.
  </action>
  <verify>
    <manual>Runbook is complete enough that another engineer can execute the inbound smoke flow locally without tribal knowledge</manual>
  </verify>
  <done>
    - The repo contains a concrete local playbook for inbound Kapso verification
    - Setup no longer depends on informal memory
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the current CRM shell surface the inbound update with minimal change</name>
  <files>
    apps/web/src/hooks/use-conversations.ts
    apps/web/src/hooks/use-conversation-messages.ts
  </files>
  <read_first>
    apps/web/src/hooks/use-conversations.ts
    apps/web/src/hooks/use-conversation-messages.ts
    apps/web/src/components/shell/detail-panel.tsx
    apps/web/src/components/shell/conversation-list.tsx
  </read_first>
  <action>
1. Run the documented smoke flow against the current frontend behavior.
2. If the inbox/detail views already surface the persisted inbound message without code changes, leave the hooks untouched and record that result in the summary/runbook.
3. If the data stays stale until a hard reload, add the smallest acceptable refresh mechanism in the existing SWR hooks:
   - prefer low-frequency revalidation or a similarly small adjustment
   - do not redesign UI components
   - do not change the `/conversations` or `/conversations/:conversationId/messages` contracts
4. Verify the final behavior against the real backend data, not fallback mocks.
  </action>
  <verify>
    <manual>Webhook -> API -> inbox/detail evidence reproduced in the current CRM shell</manual>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/web && npm run build</automated>
  </verify>
  <done>
    - The CRM shell visibly reflects the inbound persisted message
    - Any frontend change stays tightly scoped to data visibility
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && npm run build` passes after any minimal hook adjustments
2. `05.1-LOCAL-SETUP.md` documents both controlled simulation and optional real Kapso flow
3. The documented smoke flow proves:
   - webhook accepts valid signed inbound request
   - repeated event does not duplicate state
   - `/conversations` reflects the updated conversation
   - `/conversations/:conversationId/messages` returns the new inbound message
   - the CRM inbox/detail shows the same persisted data
</verification>

<success_criteria>
- KAPSO-E2E-04: the current frontend surface reflects the persisted inbound message without contract breakage
- KAPSO-E2E-05: the team can verify the full inbound flow locally today with explicit documentation
</success_criteria>
