---
phase: 05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/webhooks/dto/kapso-webhook.dto.ts
  - apps/backend/src/webhooks/webhooks.service.ts
  - apps/backend/src/webhooks/webhooks.controller.ts
  - apps/backend/src/channels/kapso/kapso.adapter.ts
  - apps/backend/src/channels/kapso/kapso.adapter.spec.ts
  - apps/backend/src/webhooks/webhooks.service.spec.ts
  - apps/backend/test/webhooks.e2e-spec.ts
autonomous: true
requirements:
  - KAPSO-E2E-01
  - KAPSO-E2E-02

must_haves:
  truths:
    - "The current Kapso webhook accepts a valid signed inbound delivery and rejects an invalid signature"
    - "Webhook id extraction works for both flattened Kapso fixtures and Meta-style nested message payloads"
    - "A real inbound payload shape reaches the queue boundary instead of being ignored before enqueue"
    - "Normalization coverage proves the inbound payload becomes one consistent internal message shape"
  artifacts:
    - path: "apps/backend/src/webhooks/webhooks.service.ts"
      provides: "Idempotent queue handoff with message id extraction compatible with real Kapso inbound payloads"
      contains: "messageId"
    - path: "apps/backend/src/channels/kapso/kapso.adapter.ts"
      provides: "Stable normalization from raw Kapso webhook payloads to the internal message contract"
      contains: "normalizeInbound"
    - path: "apps/backend/test/webhooks.e2e-spec.ts"
      provides: "HTTP-level proof that signed inbound payloads are accepted and invalid signatures are rejected"
      contains: "/webhooks/kapso"
  key_links:
    - from: "apps/backend/src/webhooks/webhooks.service.ts"
      to: "apps/backend/src/channels/kapso/kapso.adapter.ts"
      via: "the queue boundary forwards payloads that the adapter can normalize"
      pattern: "payload"
    - from: "apps/backend/src/webhooks/webhooks.controller.ts"
      to: "apps/backend/test/webhooks.e2e-spec.ts"
      via: "same signature contract must pass/reject under e2e"
      pattern: "X-Webhook-Signature"
---

<objective>
Harden inbound Kapso ingress so the existing webhook path is genuinely compatible with real signed inbound payloads.

Purpose: close the gap between the current simplified webhook tests and the payload shapes Kapso/Meta actually deliver.
Output: webhook ingress that extracts the message id from real fixtures, preserves strict signature checks, and hands real payloads to the queue/adapter path.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-CONTEXT.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-RESEARCH.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make webhook id extraction compatible with real inbound Kapso payloads</name>
  <files>
    apps/backend/src/webhooks/dto/kapso-webhook.dto.ts
    apps/backend/src/webhooks/webhooks.service.ts
    apps/backend/src/webhooks/webhooks.service.spec.ts
    apps/backend/test/webhooks.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/webhooks/webhooks.service.ts
    apps/backend/src/webhooks/dto/kapso-webhook.dto.ts
    apps/backend/src/webhooks/webhooks.service.spec.ts
    apps/backend/test/webhooks.e2e-spec.ts
  </read_first>
  <action>
1. Expand the webhook DTO and any helper logic so the service can resolve the inbound message id from:
   - the existing flattened `payload.message.id` shape
   - the primary Meta-style nested shape under `entry[].changes[].value.messages[].id`
2. Preserve the current idempotency precedence:
   - first `X-Kapso-Idempotency-Key`
   - then the best-effort message id extracted from the payload
3. Add service-level tests covering:
   - nested payload id extraction
   - flattened payload id extraction
   - duplicate skip behavior for each accepted shape
   - ignored payload only when no usable id exists anywhere
4. Extend the e2e webhook test fixtures so at least one accepted request uses the nested real-world payload shape rather than only the simplified `{ message: { id } }` fixture.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest webhooks.service.spec --runInBand</automated>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest webhooks.e2e-spec --runInBand</automated>
  </verify>
  <done>
    - Real nested inbound payloads no longer fall into the ignored path
    - Duplicate detection still works at the webhook layer
    - HTTP contract still rejects invalid signatures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Kapso normalization for real inbound fixtures</name>
  <files>
    apps/backend/src/channels/kapso/kapso.adapter.ts
    apps/backend/src/channels/kapso/kapso.adapter.spec.ts
    apps/backend/src/webhooks/webhooks.controller.ts
  </files>
  <read_first>
    apps/backend/src/channels/kapso/kapso.adapter.ts
    apps/backend/src/channels/channel.adapter.ts
    apps/backend/src/channels/kapso/normalized-message.interface.ts
    apps/backend/src/channels/kapso/kapso.adapter.spec.ts
    apps/backend/src/webhooks/webhooks.controller.ts
  </read_first>
  <action>
1. Add or extend `kapso.adapter.spec.ts` with realistic inbound fixtures that prove `normalizeInbound()` returns:
   - `externalMessageId`
   - `direction: "inbound"`
   - normalized `fromPhone`
   - correct target phone identity for `toPhone`
   - text body or `null`
   - `rawPayload` preservation
2. Tighten adapter behavior only where required by the fixtures. Do not redesign the adapter abstraction.
3. Keep webhook signature behavior strict in the controller. Only broaden acceptance if evidence shows Kapso signs the same raw bytes but with hex/base64 variance already supported by the current implementation.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest kapso.adapter.spec --runInBand</automated>
  </verify>
  <done>
    - The adapter has fixture-backed proof for the payloads this phase claims to support
    - The internal normalization contract is stable before persistence work begins
  </done>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx jest webhooks.service.spec --runInBand` passes
2. `cd apps/backend && npx jest webhooks.e2e-spec --runInBand` passes
3. `cd apps/backend && npx jest kapso.adapter.spec --runInBand` passes
4. Nested Kapso payloads resolve a webhook message id before enqueue
5. Invalid signatures still return `401`
</verification>

<success_criteria>
- KAPSO-E2E-01: ingress stays signature-protected and understands the real inbound delivery shape
- KAPSO-E2E-02: real inbound fixtures normalize cleanly to the internal message contract
</success_criteria>
