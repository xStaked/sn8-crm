---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/backend/src/channels/channel.adapter.ts
  - apps/backend/src/channels/channels.module.ts
  - apps/backend/src/channels/kapso/kapso.adapter.ts
  - apps/backend/src/channels/kapso/kapso.client.ts
  - apps/backend/src/channels/kapso/normalized-message.interface.ts
  - apps/backend/src/messaging/messaging.module.ts
  - apps/backend/src/messaging/messaging.service.ts
  - apps/backend/src/channels/kapso/kapso.adapter.spec.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "The system can send an outbound WhatsApp text through a single MessagingService facade"
    - "All outbound messaging flows through ChannelAdapter so future channels can swap in without rewriting business logic"
    - "Kapso-specific SDK wiring is isolated behind KapsoClient and KapsoAdapter"
  artifacts:
    - path: "apps/backend/src/channels/channel.adapter.ts"
      provides: "Abstract ChannelAdapter contract and NormalizedMessage export"
      exports: ["ChannelAdapter", "NormalizedMessage"]
    - path: "apps/backend/src/channels/kapso/kapso.adapter.ts"
      provides: "KapsoAdapter implementing ChannelAdapter with outbound methods"
    - path: "apps/backend/src/messaging/messaging.service.ts"
      provides: "Outbound MessagingService facade used by later bot workflows"
  key_links:
    - from: "apps/backend/src/messaging/messaging.service.ts"
      to: "ChannelAdapter"
      via: "MessagingService delegates sendText/sendTemplate to the channel abstraction"
      pattern: "channel\\.sendText|channel\\.sendTemplate"
    - from: "apps/backend/src/channels/channels.module.ts"
      to: "KapsoAdapter"
      via: "{ provide: ChannelAdapter, useClass: KapsoAdapter }"
      pattern: "provide: ChannelAdapter"
    - from: "apps/backend/src/channels/kapso/kapso.client.ts"
      to: "WhatsAppClient.messages.sendText"
      via: "Kapso SDK outbound text delivery"
      pattern: "messages\\.sendText"
---

<objective>
Implement the outbound channel foundation for Phase 1: a reusable ChannelAdapter abstraction, the KapsoAdapter/KapsoClient concrete implementation, and a MessagingService facade that later bot and approval flows can call without binding themselves to Kapso directly.

Purpose: INFRA-04. This plan isolates provider-specific WhatsApp delivery concerns from the rest of the backend before webhook ingestion work lands.
Output: Outbound messaging foundation ready to be imported by the application and reused by later phases.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChannelAdapter contract and Kapso outbound implementation</name>
  <files>
    apps/backend/src/channels/channel.adapter.ts
    apps/backend/src/channels/kapso/normalized-message.interface.ts
    apps/backend/src/channels/kapso/kapso.client.ts
    apps/backend/src/channels/kapso/kapso.adapter.ts
    apps/backend/src/channels/channels.module.ts
    apps/backend/src/channels/kapso/kapso.adapter.spec.ts
  </files>
  <behavior>
    - ChannelAdapter defines the stable outbound and normalization contract used by later phases
    - KapsoClient wraps the Kapso SDK and reads credentials from ConfigService
    - KapsoAdapter delegates outbound operations to KapsoClient and normalizes inbound Kapso payloads into `NormalizedMessage`
    - KapsoAdapter unit tests verify `sendText()` and `sendTemplate()` delegation into KapsoClient
  </behavior>
  <action>
    1. Create `apps/backend/src/channels/kapso/normalized-message.interface.ts` with the canonical normalized message shape used by inbound persistence.

    2. Create `apps/backend/src/channels/channel.adapter.ts` as an abstract class with:
       - `sendText(to: string, body: string): Promise<void>`
       - `sendTemplate(to: string, templateName: string, params: string[]): Promise<void>`
       - `normalizeInbound(rawPayload: unknown): NormalizedMessage`

    3. Create `apps/backend/src/channels/kapso/kapso.client.ts` as a thin wrapper around `@kapso/whatsapp-cloud-api` using `KAPSO_API_KEY` and `KAPSO_PHONE_NUMBER_ID` from `ConfigService`.

    4. Create `apps/backend/src/channels/kapso/kapso.adapter.ts` that extends `ChannelAdapter`, delegates outbound calls to `KapsoClient`, and implements `normalizeInbound`.

    5. Create `apps/backend/src/channels/channels.module.ts` that registers `{ provide: ChannelAdapter, useClass: KapsoAdapter }` and exports `ChannelAdapter`.

    6. Create `apps/backend/src/channels/kapso/kapso.adapter.spec.ts` verifying:
       - `KapsoAdapter.sendText()` delegates to `KapsoClient.sendText()`
       - `KapsoAdapter.sendTemplate()` delegates to `KapsoClient.sendTemplate()`
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx tsc --noEmit 2>&1 | grep -E "channels/" | head -10 || echo "no channel errors"</automated>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest kapso.adapter.spec --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ChannelAdapter exists as an abstract DI token
    - KapsoAdapter extends ChannelAdapter
    - ChannelsModule exports ChannelAdapter backed by KapsoAdapter
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessagingService outbound facade</name>
  <files>
    apps/backend/src/messaging/messaging.service.ts
    apps/backend/src/messaging/messaging.module.ts
    apps/backend/src/messaging/messaging.service.spec.ts
  </files>
  <behavior>
    - MessagingService is the application-facing outbound facade for text and template sends
    - MessagingService delegates to ChannelAdapter rather than importing Kapso-specific code
    - MessagingService unit tests verify `sendText()` and `sendTemplate()` pass through arguments unchanged
  </behavior>
  <action>
    1. Create `apps/backend/src/messaging/messaging.service.ts` with `sendText()` and `sendTemplate()` methods that delegate to injected `ChannelAdapter`.

    2. Create `apps/backend/src/messaging/messaging.module.ts` that imports `ChannelsModule`, provides `MessagingService`, and exports it for later plans.

    3. Create `apps/backend/src/messaging/messaging.service.spec.ts` verifying:
       - `sendText()` forwards the recipient and body to `ChannelAdapter.sendText()`
       - `sendTemplate()` forwards the template name and params to `ChannelAdapter.sendTemplate()`
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && npx jest messaging.service.spec --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - MessagingService provides a provider-agnostic outbound facade
    - Outbound path is test-covered from MessagingService to ChannelAdapter
  </done>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx tsc --noEmit` — no TypeScript errors in channels/messaging code
2. `cd apps/backend && npx jest messaging.service.spec` — outbound facade delegation tests pass
3. Verify `channels.module.ts` binds `ChannelAdapter` to `KapsoAdapter`
4. Verify `kapso.client.ts` calls `WhatsAppClient.messages.sendText`
5. Verify `messaging.service.ts` delegates through `ChannelAdapter` rather than importing Kapso directly
</verification>

<success_criteria>
- INFRA-04: The system can send outbound WhatsApp text via a single `MessagingService.sendText()` entrypoint
- Kapso-specific API details live behind `KapsoClient`/`KapsoAdapter`
- Later phases can inject `ChannelAdapter` or `MessagingService` without coupling to Kapso SDK details
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template at @/Users/xstaked/.claude/get-shit-done/templates/summary.md
</output>
