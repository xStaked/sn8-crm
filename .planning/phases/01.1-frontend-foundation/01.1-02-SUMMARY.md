---
phase: 01.1-frontend-foundation
plan: 02
subsystem: frontend-auth
tags: [nextjs, middleware, auth, cookies]

requires: [01.1-01]
provides:
  - Protected `/dashboard/*` routing via Next.js middleware
  - Public `/login` page with Spanish CRM copy
  - Client login form wired to `POST /auth/login`
affects: [01.1-frontend-foundation, auth-ui, session-routing]

key-files:
  created:
    - apps/web/middleware.ts
    - apps/web/src/app/login/page.tsx
    - apps/web/src/components/auth/login-form.tsx

completed: 2026-03-18
---

# Phase 1.1 Plan 02 Summary

Implemented the browser auth flow: `/login` renders the CRM sign-in card, `LoginForm` posts credentials to the Phase 1 API and redirects to `/dashboard`, and `middleware.ts` forwards the `access_token` cookie to `/auth/me` before allowing protected routes through.

## Verification

- `npm run build` passes in `apps/web`
- `middleware.ts` only matches `/dashboard/:path*`
- Login form includes the required loading and generic error states in Spanish

## Self-Check: PASSED
