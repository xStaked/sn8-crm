---
phase: 05-frontend-integration-with-current-backend
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - apps/backend/test/conversations.e2e-spec.ts
  - apps/backend/package.json
  - apps/web/src/hooks/use-conversations.ts
  - apps/web/src/hooks/use-conversation-messages.ts
  - apps/web/src/components/shell/conversation-list.tsx
  - apps/web/src/components/shell/detail-panel.tsx
autonomous: false
requirements: [FE-BE-04]

must_haves:
  truths:
    - "The login -> inbox -> detail -> logout flow is verified against the real backend"
    - "Frontend and backend contracts are exercised together, not only in isolated unit tests"
    - "Session expiration or 401 responses do not strand the user inside the dashboard"
    - "The phase ships with explicit evidence that the integration works end-to-end"
  artifacts:
    - path: "apps/backend/test/conversations.e2e-spec.ts"
      provides: "Backend contract coverage for conversation endpoints"
      contains: "/conversations"
    - path: "apps/backend/package.json"
      provides: "Repeatable seed command for browser smoke verification"
      contains: "seed:user"
  key_links:
    - from: "apps/backend/test/conversations.e2e-spec.ts"
      to: "apps/web/src/hooks/use-conversations.ts"
      via: "same response contract consumed by the frontend"
      pattern: "lastMessageAt"
    - from: "apps/web/src/hooks/use-conversation-messages.ts"
      to: "apps/backend/src/conversations/conversations.controller.ts"
      via: "message history contract"
      pattern: "conversationId"
---

<objective>
Lock down the integration with end-to-end verification instead of assuming the backend and frontend changes line up.

Purpose: Phase 5 only counts as complete if the real browser flow works against the real backend.
Output: Automated verification for the backend API surface plus a repeatable smoke path for the authenticated CRM flow.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-frontend-integration-with-current-backend/05-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-01-PLAN.md
@.planning/phases/05-frontend-integration-with-current-backend/05-02-PLAN.md
@.planning/phases/05-frontend-integration-with-current-backend/05-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete automated integration coverage</name>
  <files>
    apps/backend/test/conversations.e2e-spec.ts,
    apps/backend/package.json
  </files>
  <read_first>
    apps/backend/test/conversations.e2e-spec.ts,
    apps/backend/test/auth.e2e-spec.ts,
    apps/backend/package.json,
    prisma/seed.ts
  </read_first>
  <action>
1. Expand backend e2e coverage until it proves the real contract the frontend consumes:
   - authenticated list fetch
   - authenticated history fetch
   - unauthorized rejection
   - stable conversation ids across inbound/outbound rows
2. Add a repeatable backend seed command to `apps/backend/package.json`:
   - script name: `seed:user`
   - command should execute `prisma/seed.ts` from the repo with `ts-node`
   - the script must rely on `SEED_USER_EMAIL` and `SEED_USER_PASSWORD`, matching the seed file contract exactly
3. Use the existing frontend `build` script as the frontend verification command; do not add redundant wrapper scripts unless the executor genuinely needs them.
4. Keep the verification surface small and focused on the phase goal. This is not the place to add broad unrelated test infrastructure.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest test/conversations.e2e-spec.ts --runInBand && cd ../web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - backend e2e tests cover the full conversation read surface
    - frontend build remains green against the final integration state
    - the repo exposes one explicit repeatable user-seeding command for smoke verification
  </acceptance_criteria>
  <done>The core integration surface is covered by automated checks on both sides of the stack.</done>
</task>

<task type="auto">
  <name>Task 2: Run and document the browser-level smoke flow</name>
  <files>
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/hooks/use-conversation-messages.ts,
    apps/web/src/components/shell/conversation-list.tsx,
    apps/web/src/components/shell/detail-panel.tsx
  </files>
  <read_first>
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/hooks/use-conversation-messages.ts,
    apps/web/src/components/shell/conversation-list.tsx,
    apps/web/src/components/shell/detail-panel.tsx
  </read_first>
  <action>
Run and record the Phase 5 smoke flow against the real backend:
1. Seed a user with the explicit command from Task 1, for example:
   `SEED_USER_EMAIL=owner@example.com SEED_USER_PASSWORD=changeme npm -w apps/backend run seed:user`
2. Login with that seeded user.
3. Confirm `/dashboard` loads and the inbox shows real conversations.
4. Select a conversation and confirm real message history renders.
5. Trigger logout and confirm `/dashboard` redirects back to `/login`.
6. Force or simulate an expired session and confirm the dashboard does not stay mounted with stale data.

If a bug appears during this smoke flow, fix it in the relevant frontend file before calling the phase complete.
  </action>
  <verify>
    <manual>Login -> inbox -> detail -> logout -> unauthorized redirect verified in a browser against the current backend</manual>
  </verify>
  <acceptance_criteria>
    - login works with the current backend
    - the inbox is clearly real backend data, not canned mocks
    - selecting a row changes the detail panel with real history
    - logout and expired-session behavior return the user to `/login`
  </acceptance_criteria>
  <done>There is explicit end-to-end evidence that Phase 5 works in the browser, not just in tests.</done>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx jest test/conversations.e2e-spec.ts --runInBand` passes
2. `cd apps/web && npm run build` passes
3. Browser smoke flow proves login, inbox, detail, and logout against the live backend
</verification>

<success_criteria>
- The real CRM flow is verified from browser to backend
- Both automated and manual verification target the actual phase goal
- Phase 5 has concrete completion evidence instead of inferred confidence
</success_criteria>
