---
phase: 05-frontend-integration-with-current-backend
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web/src/lib/api.ts
  - apps/web/src/types/conversation.ts
  - apps/web/src/hooks/use-conversations.ts
  - apps/web/src/hooks/use-conversation-messages.ts
  - apps/web/src/components/shell/conversation-list.tsx
  - apps/web/src/components/shell/detail-panel.tsx
  - apps/web/src/app/dashboard/layout.tsx
  - apps/web/src/components/shell/app-sidebar.tsx
autonomous: false
requirements: [FE-BE-01, FE-BE-02, FE-BE-03]

must_haves:
  truths:
    - "The dashboard reads real conversation summaries from the backend instead of defaulting to mock data in production"
    - "Selecting a conversation loads real message history in the detail panel"
    - "Session-aware CRM navigation remains aligned with the current backend auth endpoints"
    - "The UI exposes clear loading, empty, and error states instead of silently masking failures"
  artifacts:
    - path: "apps/web/src/hooks/use-conversations.ts"
      provides: "Real inbox data hook without production mock fallback"
      contains: "/conversations"
    - path: "apps/web/src/hooks/use-conversation-messages.ts"
      provides: "Selected conversation history hook"
      contains: "/messages"
    - path: "apps/web/src/components/shell/detail-panel.tsx"
      provides: "Real message-history rendering"
      contains: "useConversationMessages"
    - path: "apps/web/src/lib/api.ts"
      provides: "Centralized API helper that handles auth failures consistently"
      contains: "credentials"
  key_links:
    - from: "apps/web/src/hooks/use-conversations.ts"
      to: "apps/backend/src/conversations/conversations.controller.ts"
      via: "GET /conversations"
      pattern: "conversations"
    - from: "apps/web/src/hooks/use-conversation-messages.ts"
      to: "apps/backend/src/conversations/conversations.controller.ts"
      via: "GET /conversations/:conversationId/messages"
      pattern: "messages"
    - from: "apps/web/src/components/shell/app-sidebar.tsx"
      to: "apps/backend/src/auth/auth.controller.ts"
      via: "DELETE /auth/logout"
      pattern: "auth/logout"
---

<objective>
Swap the CRM shell from mock data to the real backend read APIs while preserving the existing auth/session behavior.

Purpose: Finish the visible part of Phase 5 by connecting the already-built dashboard UI to the current backend for inbox summaries and detail history.
Output: A frontend that loads real conversations, renders real message history, and fails explicitly when the backend contract breaks.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-frontend-integration-with-current-backend/05-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-01-PLAN.md
@.planning/phases/05-frontend-integration-with-current-backend/05-02-PLAN.md

<interfaces>
Backend contracts:
  GET /conversations
  GET /conversations/:conversationId/messages
  DELETE /auth/logout
  GET /auth/me

Frontend constraints:
  - keep SWR as the cache layer
  - keep query-param selection (`?conversation=<id>`)
  - keep the existing 3-column shell layout
  - no production path may silently render canned mock conversations when the API fails
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mock inbox fallback with real conversation summaries</name>
  <files>
    apps/web/src/lib/api.ts,
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/components/shell/conversation-list.tsx
  </files>
  <read_first>
    apps/web/src/lib/api.ts,
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/components/shell/conversation-list.tsx,
    apps/web/middleware.ts
  </read_first>
  <action>
1. Update `apps/web/src/types/conversation.ts` to reflect the real summary contract and add any lightweight DTO types the shell needs.
2. Refactor `use-conversations.ts` to fetch real summaries through the central API helper rather than raw `fetch`.
3. Remove the current always-on mock fallback. If a local-only fallback is still needed for development, gate it behind an explicit development check and keep production behavior strict.
4. Surface loading, empty, unauthorized, and generic failure states distinctly enough that QA can tell what broke.
5. Keep the query-param driven selection model intact so the detail panel still follows `?conversation=<id>`.

Auth handling note:
- If the hook receives a 401 from the backend, route the user back to `/login` instead of leaving the shell in a broken state.
  </action>
  <verify>
    <automated>cd apps/web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `use-conversations.ts` no longer defaults to hardcoded mock data in production
    - the list renders backend-provided ids and timestamps
    - unauthorized fetches redirect or route back to `/login`
    - empty inbox state still renders correctly with zero conversations
  </acceptance_criteria>
  <done>The left-column inbox now reflects real backend data instead of mocks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the detail panel to real message history</name>
  <files>
    apps/web/src/hooks/use-conversation-messages.ts,
    apps/web/src/components/shell/detail-panel.tsx,
    apps/web/src/app/dashboard/layout.tsx,
    apps/web/src/components/shell/app-sidebar.tsx
  </files>
  <read_first>
    apps/web/src/components/shell/detail-panel.tsx,
    apps/web/src/app/dashboard/layout.tsx,
    apps/web/src/components/shell/app-sidebar.tsx
  </read_first>
  <action>
1. Add `apps/web/src/hooks/use-conversation-messages.ts` to fetch the selected conversation history via SWR.
2. Refactor `detail-panel.tsx` so it reads the selected conversation id from the URL and renders the real chronological message history instead of the current placeholder copy.
3. Preserve the current empty state when no conversation is selected.
4. Keep logout behavior intact and verify the shell still routes cleanly back to `/login`.
5. If multiple shell components need the same summary data, centralize the fetch path rather than duplicating raw requests.

UI note:
- This plan is not a redesign. Keep the current shell structure and only replace placeholder content with real backend-backed states.
  </action>
  <verify>
    <automated>cd apps/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - selecting a conversation loads real message history
    - the panel handles loading and empty-body messages gracefully
    - no placeholder Phase 2 copy remains as the primary detail state
    - logout still works from the sidebar after the refactor
  </acceptance_criteria>
  <done>The detail pane is now backed by the current backend instead of derived mock summary text.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npm run lint` passes
2. `cd apps/web && npm run build` passes
3. Dashboard loads real conversations and message history against the backend
4. Invalid or expired sessions still route to `/login`
</verification>

<success_criteria>
- Frontend inbox and detail panel use real backend data
- Production no longer hides API failures behind canned mocks
- Auth/session behavior remains intact during CRM navigation
- The existing shell UX is preserved while becoming end-to-end functional
</success_criteria>
