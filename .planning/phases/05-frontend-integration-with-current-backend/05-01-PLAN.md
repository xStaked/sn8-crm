---
phase: 05-frontend-integration-with-current-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/app.module.ts
  - apps/backend/src/conversations/conversations.module.ts
  - apps/backend/src/conversations/conversations.controller.ts
  - apps/backend/src/conversations/conversations.service.ts
  - apps/backend/src/conversations/conversations.service.spec.ts
autonomous: true
requirements: [FE-BE-02]

must_haves:
  truths:
    - "The backend exposes GET /conversations as an authenticated CRM inbox endpoint"
    - "Conversation summaries use a stable conversation id derived from participant phone identity, not Message.id"
    - "The response shape matches the frontend Conversation contract: id, contactName, lastMessage, lastMessageAt, unreadCount"
    - "Conversation ordering is newest activity first"
  artifacts:
    - path: "apps/backend/src/conversations/conversations.controller.ts"
      provides: "Authenticated GET /conversations route"
      contains: "Get('conversations')"
    - path: "apps/backend/src/conversations/conversations.service.ts"
      provides: "Conversation summary projection from Message rows"
      contains: "listConversations"
    - path: "apps/backend/src/conversations/conversations.service.spec.ts"
      provides: "Unit coverage for grouping and stable ids"
      contains: "stable conversation id"
  key_links:
    - from: "apps/backend/src/conversations/conversations.controller.ts"
      to: "apps/backend/src/auth/auth.controller.ts"
      via: "same cookie-backed JWT guard approach"
      pattern: "UseGuards"
    - from: "apps/backend/src/conversations/conversations.service.ts"
      to: "prisma/schema.prisma"
      via: "Message rows aggregated into summaries"
      pattern: "message.findMany"
---

<objective>
Add the first backend read model the frontend needs to stop depending on mock inbox data.

Purpose: Create a thin authenticated `GET /conversations` endpoint over the existing `Message` table so the CRM inbox can read real data from the current backend without waiting for a larger CRM schema rewrite.
Output: A NestJS conversations module that projects `Message` rows into stable conversation summaries ordered by latest activity.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-frontend-integration-with-current-backend/05-RESEARCH.md

<interfaces>
Current persisted shape (from prisma/schema.prisma):
  model Message {
    id                String
    externalMessageId String
    direction         String
    fromPhone         String
    toPhone           String
    body              String?
    channel           String
    createdAt         DateTime
  }

Required API shape:
  GET /conversations
    200 [
      {
        id: string,
        contactName: string,
        lastMessage: string,
        lastMessageAt: string,
        unreadCount: number
      }
    ]

Stable conversation key rule:
  - inbound message   -> conversation id is normalized fromPhone
  - outbound message  -> conversation id is normalized toPhone
  - use the same derivation in every query path

Auth requirement:
  - endpoint must require the same cookie-backed JWT session used by GET /auth/me
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a conversations module with authenticated GET /conversations</name>
  <files>
    apps/backend/src/app.module.ts,
    apps/backend/src/conversations/conversations.module.ts,
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.service.ts
  </files>
  <read_first>
    apps/backend/src/app.module.ts,
    apps/backend/src/auth/auth.controller.ts,
    apps/backend/src/messaging/processors/message.processor.ts,
    prisma/schema.prisma,
    .planning/phases/05-frontend-integration-with-current-backend/05-RESEARCH.md
  </read_first>
  <action>
1. Create a new `conversations` backend module and import it from `apps/backend/src/app.module.ts`.
2. Add `apps/backend/src/conversations/conversations.controller.ts` with an authenticated `GET /conversations` route protected by the existing JWT guard pattern.
3. Add `apps/backend/src/conversations/conversations.service.ts` that reads `Message` rows via Prisma and folds them into one summary per stable conversation id.
4. Implement the stable conversation id in exactly one shared helper inside the service. Do not duplicate the derivation logic in the controller.
5. For Phase 5, set `contactName` to the normalized participant phone string. Do not parse provider-specific names from `rawPayload` in this phase.
6. Set `unreadCount` to `0` for now. Do not fabricate unread state without a read cursor source of truth.

Implementation notes:
- The service may use `prisma.message.findMany({ orderBy: { createdAt: 'desc' } })` and fold in TypeScript. The project scale is small enough that a simple, explicit projection is preferable to clever SQL here.
- Only the newest message per conversation should produce the summary row.
- `lastMessage` should be a safe string. Use `body ?? ''` and let the frontend render an empty-state preview if needed.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest src/conversations/conversations.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `GET /conversations` exists and is guarded by JWT auth
    - response items contain `id`, `contactName`, `lastMessage`, `lastMessageAt`, `unreadCount`
    - summaries are ordered by newest `createdAt` first
    - one conversation id is emitted for mixed inbound/outbound traffic with the same participant
    - no summary uses `Message.id` as the conversation id
    - `contactName` is always the normalized participant phone string in this phase
  </acceptance_criteria>
  <done>The backend can now return a real CRM inbox summary list derived from persisted messages.</done>
</task>

<task type="auto">
  <name>Task 2: Add service-level unit tests for grouping and ordering</name>
  <files>apps/backend/src/conversations/conversations.service.spec.ts</files>
  <read_first>
    apps/backend/src/auth/auth.service.spec.ts,
    apps/backend/src/messaging/processors/message.processor.spec.ts
  </read_first>
  <action>
Create `apps/backend/src/conversations/conversations.service.spec.ts` that covers:
- grouping multiple inbound messages from the same phone into one summary
- grouping inbound and outbound messages for the same participant into one stable conversation id
- newest activity sorts first
- null message bodies do not crash the projection
- unreadCount defaults to 0

Mock Prisma rather than touching the database.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest src/conversations/conversations.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - tests fail if grouping uses `Message.id`
    - tests fail if ordering is ascending instead of descending
    - tests cover both `direction: 'inbound'` and `direction: 'outbound'`
  </acceptance_criteria>
  <done>Conversation summary behavior is locked down with targeted unit tests before the frontend depends on it.</done>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx jest src/conversations/conversations.service.spec.ts --runInBand` passes
2. `GET /conversations` is registered under the Nest app and requires auth
3. Response DTO shape matches the frontend summary contract exactly
</verification>

<success_criteria>
- Backend exposes authenticated real inbox summaries
- Stable conversation ids are based on participant identity
- Summary projection is tested and ordered correctly
- The frontend has a concrete API to replace mock list data
</success_criteria>
