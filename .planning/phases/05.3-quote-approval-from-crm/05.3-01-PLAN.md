---
phase: 05.3-quote-approval-from-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/conversations/dto/conversation-response.dto.ts
  - apps/backend/src/conversations/dto/quote-review-response.dto.ts
  - apps/backend/src/conversations/conversations.service.ts
  - apps/backend/src/conversations/conversations.controller.ts
  - apps/backend/src/conversations/conversations.service.spec.ts
  - apps/backend/src/conversations/conversations.controller.spec.ts
  - apps/backend/test/conversations.e2e-spec.ts
autonomous: true
requirements: [QUOTE-CRM-01, QUOTE-CRM-02]

must_haves:
  truths:
    - "GET /conversations remains additive and can expose pending quote review metadata without breaking the existing inbox contract."
    - "GET /conversations/:conversationId/quote-review returns the latest actionable draft preview for a conversation, including rendered quote and business context."
    - "The CRM can tell whether a conversation has a pending quote review using the same stable conversation id already used by inbox and detail."
  artifacts:
    - path: "apps/backend/src/conversations/dto/quote-review-response.dto.ts"
      provides: "Typed DTOs for pending quote summary metadata and conversation quote review detail."
      contains: "export class ConversationQuoteReviewDto"
    - path: "apps/backend/src/conversations/conversations.service.ts"
      provides: "Quote review read model backed by QuoteDraft and CommercialBrief."
      contains: "async getConversationQuoteReview"
    - path: "apps/backend/src/conversations/conversations.controller.ts"
      provides: "Authenticated quote review detail endpoint."
      contains: "@Get('conversations/:conversationId/quote-review')"
  key_links:
    - from: "apps/backend/src/conversations/conversations.controller.ts"
      to: "apps/backend/src/conversations/conversations.service.ts"
      via: "getConversationQuoteReview(conversationId)"
      pattern: "this\\.conversationsService\\.getConversationQuoteReview"
    - from: "apps/backend/src/conversations/conversations.service.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "QuoteDraft + CommercialBrief query filtered by conversationId and reviewStatus"
      pattern: "quoteDraft\\.findFirst"
    - from: "apps/backend/src/conversations/conversations.service.ts"
      to: "apps/backend/src/conversations/dto/conversation-response.dto.ts"
      via: "pendingQuote summary metadata added without replacing existing fields"
      pattern: "pendingQuote"
---

<objective>
Expose quote-review state to the CRM through additive conversation read contracts: pending metadata on `GET /conversations` and a new detail preview endpoint on the selected conversation.

Purpose: Give the existing CRM shell enough backend data to show a pending-review inbox signal and a decision-ready quote preview without inventing a new quote module or route.
Output: Authenticated read endpoints and tests for pending quote metadata plus conversation-scoped quote preview.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-01-SUMMARY.md
@.planning/phases/05-frontend-integration-with-current-backend/05-02-SUMMARY.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-02-SUMMARY.md

<interfaces>
From apps/backend/src/conversations/dto/conversation-response.dto.ts:
```typescript
export class ConversationSummaryDto {
  id: string;
  contactName: string;
  lastMessage: string;
  lastMessageAt: string;
  unreadCount: number;
}

export class ConversationMessageDto {
  id: string;
  conversationId: string;
  direction: 'inbound' | 'outbound';
  body: string | null;
  createdAt: string;
}
```

From apps/backend/src/conversations/conversations.service.ts:
```typescript
type ConversationSummary = {
  id: string;
  contactName: string;
  lastMessage: string;
  lastMessageAt: string;
  unreadCount: number;
};

async listConversations(): Promise<ConversationSummary[]>
async listConversationMessages(conversationId: string): Promise<ConversationMessage[]>
```

From apps/backend/prisma/schema.prisma:
```prisma
model QuoteDraft {
  conversationId        String
  version               Int
  reviewStatus          QuoteReviewStatus
  draftPayload          Json
  renderedQuote         String?
  ownerFeedbackSummary  String?
  approvedAt            DateTime?
  deliveredToCustomerAt DateTime?
  commercialBrief       CommercialBrief
}

model CommercialBrief {
  conversationId String @unique
  customerName   String?
  projectType    String?
  budget         String?
  urgency        String?
  summary        String?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conversation-scoped quote review read DTOs and service queries</name>
  <files>
    apps/backend/src/conversations/dto/conversation-response.dto.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts,
    apps/backend/src/conversations/conversations.service.ts
  </files>
  <read_first>
    apps/backend/src/conversations/dto/conversation-response.dto.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/prisma/schema.prisma,
    apps/backend/src/ai-sales/owner-review.service.ts,
    .planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
  </read_first>
  <action>
Add an additive read model in `ConversationsService` that exposes quote review state without changing the stable conversation-id contract.

1. Create `apps/backend/src/conversations/dto/quote-review-response.dto.ts` with:
   - `PendingQuoteSummaryDto`
   - `ConversationQuoteReviewBriefDto`
   - `ConversationQuoteReviewDto`
   Use exact fields from research: `conversationId`, `quoteDraftId`, `version`, `reviewStatus`, `renderedQuote`, `draftSummary`, `ownerFeedbackSummary`, `approvedAt`, `deliveredToCustomerAt`, and a nested `commercialBrief` object with `customerName`, `summary`, `projectType`, `budget`, and `urgency`.

2. Extend `ConversationSummaryDto` and the local `ConversationSummary` type with an additive `pendingQuote: PendingQuoteSummaryDto | null` field. Preserve all existing fields and do not rename or remove anything from the current `/conversations` contract.

3. In `ConversationsService`, add a dedicated helper that finds the latest review-relevant `QuoteDraft` for a conversation using the existing stable phone-based conversation id. Treat these statuses as review-relevant: `pending_owner_review`, `ready_for_recheck`, `changes_requested`, `approved`, `delivered_to_customer`. Use `renderedQuote` as the primary preview body and `draftPayload.summary` as the summary fallback.

4. Update `listConversations()` so each conversation summary includes `pendingQuote` when the latest review-relevant draft is still actionable in CRM. Keep the existing newest-message ordering and participant-phone grouping from Phase 05.

5. Add `getConversationQuoteReview(conversationId: string)` returning `ConversationQuoteReviewDto`. If the conversation exists but has no draft in the review flow, throw `NotFoundException`. Do not create a second quote renderer; read from persisted `QuoteDraft` and `CommercialBrief`.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/conversations/dto/quote-review-response.dto.ts` exists
    - `apps/backend/src/conversations/dto/quote-review-response.dto.ts` contains `export class ConversationQuoteReviewDto`
    - `apps/backend/src/conversations/dto/conversation-response.dto.ts` contains `pendingQuote`
    - `apps/backend/src/conversations/conversations.service.ts` contains `async getConversationQuoteReview`
    - `apps/backend/src/conversations/conversations.service.ts` reads `renderedQuote`
    - `apps/backend/src/conversations/conversations.service.ts` reads `ownerFeedbackSummary`
    - `apps/backend/src/conversations/conversations.service.ts` returns `deliveredToCustomerAt`
    - `npx jest apps/backend/src/conversations/conversations.service.spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The service can now project pending quote metadata into inbox summaries and return a decision-ready quote preview DTO for a selected conversation.</done>
</task>

<task type="auto">
  <name>Task 2: Expose authenticated quote review read endpoints and additive contract tests</name>
  <files>
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.service.spec.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts
  </read_first>
  <action>
Expose the read model through authenticated conversation routes and prove the additive contract with controller and API-boundary tests.

1. Add `GET /conversations/:conversationId/quote-review` to `ConversationsController` behind `JwtAuthGuard`, with Swagger metadata aligned to the existing conversation endpoints.
2. Keep `GET /conversations` backward-compatible while documenting the optional `pendingQuote` field in the DTO/Swagger response.
3. Extend controller/service specs to prove:
   - summaries still return the existing fields and now optionally include `pendingQuote`
   - quote review detail resolves with the same stable `conversationId` contract used by inbox and message history
   - missing review drafts return 404
4. Extend `apps/backend/test/conversations.e2e-spec.ts` so the authenticated contract covers both the additive `/conversations` metadata and the new `/conversations/:conversationId/quote-review` endpoint. Preserve the existing `/conversations` behavior; this phase must not break callers that only know the Phase 05 contract.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/conversations/conversations.controller.ts` contains `@Get('conversations/:conversationId/quote-review')`
    - `apps/backend/src/conversations/conversations.controller.ts` contains `type: ConversationQuoteReviewDto`
    - `apps/backend/src/conversations/conversations.controller.spec.ts` asserts `getConversationQuoteReview`
    - `apps/backend/test/conversations.e2e-spec.ts` contains `/conversations/:conversationId/quote-review`
    - `apps/backend/test/conversations.e2e-spec.ts` asserts `pendingQuote`
    - `npx jest apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The backend exposes authenticated quote review reads through the existing conversations surface, and automated tests prove the additive contract does not break current inbox consumers.</done>
</task>

</tasks>

<verification>
Run the conversation service spec, controller spec, and API e2e coverage for the additive read contracts:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.service.spec.ts apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand`
</verification>

<success_criteria>
`GET /conversations` remains backward-compatible while exposing `pendingQuote` metadata, and `GET /conversations/:conversationId/quote-review` returns the latest review-ready draft preview using persisted `QuoteDraft` and `CommercialBrief` data.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-quote-approval-from-crm/05.3-01-SUMMARY.md`.
</output>
