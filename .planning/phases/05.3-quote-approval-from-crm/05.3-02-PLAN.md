---
phase: 05.3-quote-approval-from-crm
plan: 02
type: execute
wave: 2
depends_on: [05.3-01]
files_modified:
  - apps/backend/src/ai-sales/owner-review.service.ts
  - apps/backend/src/ai-sales/owner-review.service.spec.ts
  - apps/backend/src/conversations/dto/approve-quote.dto.ts
  - apps/backend/src/conversations/dto/request-quote-changes.dto.ts
  - apps/backend/src/conversations/dto/quote-review-response.dto.ts
  - apps/backend/src/conversations/conversations.service.ts
  - apps/backend/src/conversations/conversations.controller.ts
  - apps/backend/src/conversations/conversations.service.spec.ts
  - apps/backend/src/conversations/conversations.controller.spec.ts
  - apps/backend/test/conversations.e2e-spec.ts
autonomous: true
requirements: [QUOTE-CRM-03, QUOTE-CRM-04]

must_haves:
  truths:
    - "The CRM can approve the active draft version for a conversation without bypassing OwnerReviewService."
    - "The CRM can request changes with required comments, and the request is recorded as an auditable review event."
    - "Approvals still trigger the existing automatic customer delivery path and persist delivery timing for CRM visibility."
  artifacts:
    - path: "apps/backend/src/conversations/dto/approve-quote.dto.ts"
      provides: "Versioned approve request body DTO."
      contains: "export class ApproveQuoteDto"
    - path: "apps/backend/src/conversations/dto/request-quote-changes.dto.ts"
      provides: "Versioned request-changes DTO with required feedback."
      contains: "export class RequestQuoteChangesDto"
    - path: "apps/backend/src/ai-sales/owner-review.service.ts"
      provides: "Transport-aware CRM approval and revision delegation with delivery timestamp handling."
      contains: "deliveredToCustomerAt"
  key_links:
    - from: "apps/backend/src/conversations/conversations.service.ts"
      to: "apps/backend/src/ai-sales/owner-review.service.ts"
      via: "approve/request changes delegation"
      pattern: "ownerReviewService"
    - from: "apps/backend/src/ai-sales/owner-review.service.ts"
      to: "apps/backend/src/ai-sales/owner-review.service.ts"
      via: "prepareApprovedCustomerDelivery reuse after approval"
      pattern: "prepareApprovedCustomerDelivery"
    - from: "apps/backend/src/ai-sales/owner-review.service.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "persist deliveredToCustomerAt on successful customer delivery"
      pattern: "deliveredToCustomerAt"
---

<objective>
Expose CRM write actions for quote approval and revision requests by delegating to the existing `OwnerReviewService`, while making the audit trail transport-correct and preserving the already-implemented automatic customer delivery path.

Purpose: Let the web CRM drive the existing approval engine instead of maintaining a parallel approval flow or sending customer messages directly from the controller.
Output: Authenticated conversation-scoped approve/request-changes endpoints with DTO validation, service delegation, delivery timestamp persistence, and tests.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
@.planning/phases/05.2-manual-reply-from-crm/05.2-01-SUMMARY.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-01-PLAN.md

<interfaces>
From apps/backend/src/ai-sales/dto/owner-review-command.dto.ts:
```typescript
export enum OwnerReviewAction {
  APPROVE = 'approve',
  REVISE = 'revise',
}

export class OwnerReviewCommandDto {
  action!: OwnerReviewAction;
  conversationId!: string;
  version!: number;
  reviewerPhone?: string;
  feedback?: string;
}
```

From apps/backend/src/ai-sales/owner-review.service.ts:
```typescript
async approveDraft(command: OwnerReviewCommandDto): Promise<void>
async requestChanges(command: OwnerReviewCommandDto): Promise<void>
async prepareApprovedCustomerDelivery(conversationId: string): Promise<ApprovedCustomerDeliveryPayload>
```

From apps/backend/prisma/schema.prisma:
```prisma
model QuoteDraft {
  reviewStatus          QuoteReviewStatus @default(pending_owner_review)
  ownerFeedbackSummary  String?
  approvedAt            DateTime?
  deliveredToCustomerAt DateTime?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make OwnerReviewService transport-aware for CRM actions and delivery audit fields</name>
  <files>
    apps/backend/src/ai-sales/owner-review.service.ts,
    apps/backend/src/ai-sales/owner-review.service.spec.ts
  </files>
  <read_first>
    apps/backend/src/ai-sales/owner-review.service.ts,
    apps/backend/src/ai-sales/owner-review.service.spec.ts,
    apps/backend/src/ai-sales/dto/owner-review-command.dto.ts,
    apps/backend/prisma/schema.prisma,
    .planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
  </read_first>
  <action>
Extend the existing owner review engine for CRM-originated actions instead of introducing a second approval path.

1. Refactor `OwnerReviewService` so approval/revision audit text can distinguish the source transport. Use explicit source metadata such as `crm` vs `whatsapp`, and keep the WhatsApp command path intact.
2. Add thin CRM-safe entrypoints or extend the existing methods so the conversations layer can call approval and request-changes without going through `handleOwnerCommand()`.
3. On approval, keep `prepareApprovedCustomerDelivery()` as the single customer-delivery source. Do not send customer messages from the conversations controller or service.
4. When customer delivery succeeds, update the approved draft with both `reviewStatus: 'delivered_to_customer'` and `deliveredToCustomerAt: new Date()`.
5. Extend `owner-review.service.spec.ts` to prove:
   - CRM approvals do not write `via WhatsApp command` into the review event feedback
   - CRM approvals still call the same customer delivery send path
   - `deliveredToCustomerAt` is written when the delivery send succeeds
   - request-changes still records the owner feedback and enqueues regeneration
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/ai-sales/owner-review.service.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/ai-sales/owner-review.service.ts` contains transport-aware CRM approval or revision entrypoints
    - `apps/backend/src/ai-sales/owner-review.service.ts` still contains `prepareApprovedCustomerDelivery`
    - `apps/backend/src/ai-sales/owner-review.service.ts` contains `deliveredToCustomerAt`
    - `apps/backend/src/ai-sales/owner-review.service.spec.ts` asserts CRM-origin audit text is not WhatsApp-specific
    - `apps/backend/src/ai-sales/owner-review.service.spec.ts` asserts `deliveredToCustomerAt: expect.any(Date)`
    - `npx jest apps/backend/src/ai-sales/owner-review.service.spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The approval engine remains single-sourced in `OwnerReviewService`, CRM actions are auditable as CRM-originated, and successful approvals persist delivery timing for the CRM read model.</done>
</task>

<task type="auto">
  <name>Task 2: Add conversation-scoped approve and request-changes endpoints that delegate to OwnerReviewService</name>
  <files>
    apps/backend/src/conversations/dto/approve-quote.dto.ts,
    apps/backend/src/conversations/dto/request-quote-changes.dto.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts,
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.service.spec.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts
  </files>
  <read_first>
    apps/backend/src/conversations/conversations.service.ts,
    apps/backend/src/conversations/conversations.controller.ts,
    apps/backend/src/conversations/conversations.service.spec.ts,
    apps/backend/src/conversations/conversations.controller.spec.ts,
    apps/backend/test/conversations.e2e-spec.ts,
    apps/backend/src/ai-sales/owner-review.service.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts
  </read_first>
  <action>
Add authenticated CRM write actions under the existing conversations surface.

1. Create `ApproveQuoteDto` with a required integer `version >= 1`.
2. Create `RequestQuoteChangesDto` with required `version >= 1` and non-empty `feedback` capped to a reasonable CRM-safe length.
3. Inject `OwnerReviewService` into `ConversationsService` and add two methods:
   - `approveConversationQuote(conversationId, dto, reviewerIdentity)`
   - `requestConversationQuoteChanges(conversationId, dto, reviewerIdentity)`
   Use the same normalized stable conversation id as the read endpoints.
4. Add controller routes:
   - `POST /conversations/:conversationId/quote-review/approve`
   - `POST /conversations/:conversationId/quote-review/request-changes`
   Both must be behind `JwtAuthGuard`.
5. After each action, return the refreshed quote-review detail DTO from `getConversationQuoteReview()` so the frontend can re-render immediately without inventing a second response shape.
6. Extend service/controller/e2e tests to prove:
   - approve delegates through `OwnerReviewService`
   - request-changes requires feedback
   - stale or missing versions fail cleanly
   - the approve route returns `deliveredToCustomerAt` after the automatic delivery path runs
   - the existing `/conversations` and `/conversations/:conversationId/messages` contracts still pass
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/conversations/conversations.service.spec.ts apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/conversations/dto/approve-quote.dto.ts` exists
    - `apps/backend/src/conversations/dto/request-quote-changes.dto.ts` exists
    - `apps/backend/src/conversations/conversations.controller.ts` contains `@Post('conversations/:conversationId/quote-review/approve')`
    - `apps/backend/src/conversations/conversations.controller.ts` contains `@Post('conversations/:conversationId/quote-review/request-changes')`
    - `apps/backend/src/conversations/conversations.service.ts` contains `approveConversationQuote`
    - `apps/backend/src/conversations/conversations.service.ts` contains `requestConversationQuoteChanges`
    - `apps/backend/src/conversations/conversations.service.ts` references `OwnerReviewService`
    - `apps/backend/test/conversations.e2e-spec.ts` contains both quote-review POST routes
    - `apps/backend/test/conversations.e2e-spec.ts` asserts `deliveredToCustomerAt`
    - `npx jest apps/backend/src/conversations/conversations.service.spec.ts apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand` exits 0
  </acceptance_criteria>
  <done>The backend now accepts CRM-originated quote approvals and revision requests through `/conversations`, while all state transitions and customer delivery remain delegated to `OwnerReviewService`.</done>
</task>

</tasks>

<verification>
Run the owner-review service tests plus conversation service/controller/e2e coverage:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && npx jest apps/backend/src/ai-sales/owner-review.service.spec.ts apps/backend/src/conversations/conversations.service.spec.ts apps/backend/src/conversations/conversations.controller.spec.ts apps/backend/test/conversations.e2e-spec.ts --runInBand`
</verification>

<success_criteria>
The CRM can approve or request changes on the active draft version through authenticated `/conversations` routes, and successful approval still uses the pre-existing automatic customer delivery path while persisting CRM-visible audit timestamps.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-quote-approval-from-crm/05.3-02-SUMMARY.md`.
</output>
