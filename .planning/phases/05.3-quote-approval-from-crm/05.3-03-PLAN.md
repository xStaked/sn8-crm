---
phase: 05.3-quote-approval-from-crm
plan: 03
type: execute
wave: 3
depends_on: [05.3-01, 05.3-02]
files_modified:
  - apps/web/src/types/conversation.ts
  - apps/web/src/hooks/use-conversations.ts
  - apps/web/src/hooks/use-conversation-messages.ts
  - apps/web/src/hooks/use-conversation-quote-review.ts
  - apps/web/src/components/shell/conversation-list.tsx
  - apps/web/src/components/shell/detail-panel.tsx
autonomous: true
requirements: [QUOTE-CRM-01, QUOTE-CRM-02, QUOTE-CRM-03, QUOTE-CRM-04]

must_haves:
  truths:
    - "The CRM inbox shows which conversations have a pending quote review without leaving the existing shell."
    - "The detail panel can preview the latest quote draft and business summary before approval."
    - "The user can approve or request changes from the detail panel, and approval reflects the automatic customer delivery result in the same UI."
  artifacts:
    - path: "apps/web/src/hooks/use-conversation-quote-review.ts"
      provides: "SWR hook for quote review detail state and revalidation."
      contains: "export function useConversationQuoteReview"
    - path: "apps/web/src/components/shell/conversation-list.tsx"
      provides: "Pending quote badge or metadata in the inbox list."
      contains: "pendingQuote"
    - path: "apps/web/src/components/shell/detail-panel.tsx"
      provides: "Quote review card above the manual composer with approve/request-changes actions."
      contains: "Aprobar y enviar"
  key_links:
    - from: "apps/web/src/hooks/use-conversations.ts"
      to: "/conversations"
      via: "typed summary fetch including pendingQuote"
      pattern: "pendingQuote"
    - from: "apps/web/src/hooks/use-conversation-quote-review.ts"
      to: "/conversations/:conversationId/quote-review"
      via: "SWR detail fetch"
      pattern: "quote-review"
    - from: "apps/web/src/components/shell/detail-panel.tsx"
      to: "/conversations/:conversationId/quote-review/approve"
      via: "approve action and SWR mutate"
      pattern: "quote-review/approve"
---

<objective>
Integrate quote approval into the existing CRM shell by surfacing pending-review metadata in the inbox and rendering a quote review card inside `DetailPanel` above the manual reply composer.

Purpose: Close the operational loop in the current `/conversations` workflow so the owner can review, approve, or request changes without leaving the inbox/detail layout or depending on WhatsApp review notifications.
Output: Typed frontend contracts, a quote review SWR hook, inbox badges, and detail-panel actions wired to the new backend endpoints.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
@.planning/phases/05-frontend-integration-with-current-backend/05-03-SUMMARY.md
@.planning/phases/05.1-integraci-n-real-de-kapso-y-flujo-inbound-end-to-end/05.1-03-SUMMARY.md
@.planning/phases/05.2-manual-reply-from-crm/05.2-01-SUMMARY.md
@.planning/phases/05.2-manual-reply-from-crm/05.2-02-PLAN.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-01-PLAN.md
@.planning/phases/05.3-quote-approval-from-crm/05.3-02-PLAN.md

<interfaces>
From apps/web/src/types/conversation.ts:
```typescript
export type Conversation = {
  id: string;
  contactName: string;
  lastMessage: string;
  lastMessageAt: string;
  unreadCount: number;
};

export type ConversationMessage = {
  id: string;
  conversationId: string;
  direction: "inbound" | "outbound";
  body: string | null;
  createdAt: string;
};
```

From apps/web/src/hooks/use-conversations.ts:
```typescript
const CONVERSATIONS_ENDPOINT = "/conversations";
export function useConversations() {
  const { data, error, isLoading } = useSWR<ConversationSummaryDto[]>(CONVERSATIONS_ENDPOINT, fetcher, ...);
  return { conversations, error, isLoading, state };
}
```

From apps/web/src/components/shell/detail-panel.tsx:
```tsx
const { messages, error, state, mutate } = useConversationMessages(selectedId);
async function handleSend() { ... }
```

Backend contracts from Phase 05.3:
```typescript
GET /conversations
// additive pendingQuote?: { version, reviewStatus, updatedAt } | null

GET /conversations/:conversationId/quote-review
POST /conversations/:conversationId/quote-review/approve
POST /conversations/:conversationId/quote-review/request-changes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend conversation types and hooks for quote review state</name>
  <files>
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/hooks/use-conversation-quote-review.ts
  </files>
  <read_first>
    apps/web/src/types/conversation.ts,
    apps/web/src/hooks/use-conversations.ts,
    apps/web/src/lib/api.ts,
    apps/backend/src/conversations/dto/quote-review-response.dto.ts,
    .planning/phases/05.3-quote-approval-from-crm/05.3-RESEARCH.md
  </read_first>
  <action>
Add typed frontend support for the additive quote review contracts without changing the current shell fetch patterns.

1. Extend `apps/web/src/types/conversation.ts` with:
   - `PendingQuoteSummary`
   - `ConversationQuoteReview`
   - any UI state union needed for quote review loading/error/empty handling
   Add `pendingQuote?: PendingQuoteSummary | null` to the existing `Conversation` / `ConversationSummaryDto` shape.
2. Keep `useConversations()` on the same `/conversations` endpoint and preserve the existing auth-expiry redirect behavior. Only widen the typed response so the hook can pass `pendingQuote` through to the shell.
3. Create `apps/web/src/hooks/use-conversation-quote-review.ts` using SWR and the existing `apiFetchJson` / `isApiError` conventions. Match the established 5-second refresh pattern unless the current code shows a better localized convention.
4. The new hook must return `{ quoteReview, error, isLoading, state, mutate }` so `DetailPanel` can revalidate after approve/request-changes.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/types/conversation.ts` contains `PendingQuoteSummary`
    - `apps/web/src/types/conversation.ts` contains `ConversationQuoteReview`
    - `apps/web/src/types/conversation.ts` adds `pendingQuote`
    - `apps/web/src/hooks/use-conversation-quote-review.ts` exists
    - `apps/web/src/hooks/use-conversation-quote-review.ts` contains `export function useConversationQuoteReview`
    - `apps/web/src/hooks/use-conversation-quote-review.ts` returns `mutate`
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The frontend can type and fetch pending quote metadata plus quote review detail using the same SWR/auth conventions as the existing shell.</done>
</task>

<task type="auto">
  <name>Task 2: Render quote review affordances in the inbox and detail panel without redesigning the shell</name>
  <files>
    apps/web/src/hooks/use-conversation-messages.ts,
    apps/web/src/components/shell/conversation-list.tsx,
    apps/web/src/components/shell/detail-panel.tsx
  </files>
  <read_first>
    apps/web/src/components/shell/conversation-list.tsx,
    apps/web/src/components/shell/detail-panel.tsx,
    apps/web/src/hooks/use-conversation-messages.ts,
    apps/web/src/hooks/use-conversation-quote-review.ts,
    apps/web/src/types/conversation.ts,
    .agents/skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
Keep the UI additive to the current shell and follow the existing dark CRM styling rather than introducing a new route or redesign.

1. In `conversation-list.tsx`, add a lightweight pending-review badge or inline metadata when `conversation.pendingQuote` exists. Keep the current query-string selection model unchanged.
2. In `detail-panel.tsx`, add a quote review card below the header and above the message history/composer stack when a selected conversation has review data. The card must include:
   - status badge with text, not color alone
   - draft version
   - brief context fields (customer, summary, budget, urgency, project type when present)
   - rendered quote preview in a scroll-contained area
   - latest owner feedback summary when present
   - primary action button labeled exactly `Aprobar y enviar`
   - secondary action button labeled exactly `Solicitar revisión`
   - textarea for revision comments shown when the user is requesting changes
3. Wire the buttons to:
   - `POST /conversations/:conversationId/quote-review/approve`
   - `POST /conversations/:conversationId/quote-review/request-changes`
   Reuse `apiFetchJson`, keep loading/disabled states explicit, and on success call `mutate()` on both the quote-review hook and the conversations list hook so inbox/detail stay consistent.
4. Preserve the manual reply compose area from Phase 05.2 as a sibling section below the conversation history. Do not replace it with the approval UI.
5. Follow the UI skill constraints relevant here:
   - visible action hierarchy with one primary CTA
   - keyboard-focusable controls and non-color-only state cues
   - no hover-only critical interactions
   - maintain mobile-safe readable text sizing and avoid nested scroll bugs
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/shell/conversation-list.tsx` contains `pendingQuote`
    - `apps/web/src/components/shell/detail-panel.tsx` contains `Aprobar y enviar`
    - `apps/web/src/components/shell/detail-panel.tsx` contains `Solicitar revisión`
    - `apps/web/src/components/shell/detail-panel.tsx` contains `/quote-review/approve`
    - `apps/web/src/components/shell/detail-panel.tsx` contains `/quote-review/request-changes`
    - `apps/web/src/components/shell/detail-panel.tsx` renders the review card above the composer area
    - `apps/web/src/components/shell/detail-panel.tsx` still contains the manual reply send handler or compose UI from Phase 05.2
    - `apps/web/src/components/shell/detail-panel.tsx` references both quote-review mutate and conversations mutate after action success
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The current CRM shell now shows pending-review signals in the inbox and lets the owner preview, approve, or request changes inside `DetailPanel` while preserving the existing message composer below it.</done>
</task>

</tasks>

<verification>
Type-check the frontend after integrating the new quote review hook, list metadata, and detail-panel actions:

`cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm && pnpm --filter web exec tsc --noEmit`
</verification>

<success_criteria>
The existing `/conversations` shell exposes pending quote reviews in the inbox and renders a usable approval card in the detail pane, with approve/request-changes actions wired to the backend and approval reflecting automatic customer delivery status.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-quote-approval-from-crm/05.3-03-SUMMARY.md`.
</output>
