---
phase: 02.1-ai-sales-agent-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/backend/src/app.module.ts
  - apps/backend/src/ai-sales/ai-sales.module.ts
  - apps/backend/src/ai-sales/ai-sales.service.ts
  - apps/backend/src/ai-sales/ai-provider.interface.ts
  - apps/backend/src/ai-sales/deepseek/deepseek.client.ts
  - apps/backend/src/ai-sales/prompts/sales-agent.system.ts
  - apps/backend/src/ai-sales/prompts/quote-template.contract.ts
  - .env.example
autonomous: false
requirements:
  - SALES-AI-01
  - SALES-AI-02
  - SALES-AI-03
  - SALES-AI-05

must_haves:
  truths:
    - "The backend has a dedicated AI sales module instead of burying LLM logic inside webhook or messaging transport code"
    - "Commercial brief state and quote-draft state have durable persistence separate from raw Message rows"
    - "DeepSeek access is hidden behind a small provider contract so prompts and orchestration do not couple directly to transport details"
    - "The owner-defined quotation format has an explicit configuration surface and cannot stay an invisible prompt string"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Durable AI sales state models keyed by conversation identity"
      contains: "CommercialBrief"
    - path: "apps/backend/src/ai-sales/ai-provider.interface.ts"
      provides: "Stable provider contract for structured brief + quote generation"
      contains: "AiProvider"
    - path: "apps/backend/src/ai-sales/prompts/quote-template.contract.ts"
      provides: "Explicit owner-quotation format contract used by generation logic"
      contains: "QUOTE_TEMPLATE"
  key_links:
    - from: "apps/backend/src/ai-sales/ai-sales.module.ts"
      to: "apps/backend/src/app.module.ts"
      via: "NestJS module imports"
      pattern: "AiSalesModule"
    - from: "apps/backend/src/ai-sales/ai-sales.service.ts"
      to: "prisma/schema.prisma"
      via: "Persistent brief/draft models"
      pattern: "CommercialBrief|QuoteDraft"
---

<objective>
Create the domain foundation for the AI sales agent: persistent state, provider abstraction, DeepSeek configuration surface, and a first-class quotation-template contract.

Purpose: make the AI phase implementable on top of the existing messaging substrate instead of trying to improvise state from raw transcripts.
Output: backend module skeleton, durable models for brief/draft/review state, env/config surface for DeepSeek, and an explicit quotation-format contract tied to owner input.
</objective>

<execution_context>
@/Users/xstaked/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.1-ai-sales-agent-configuration/02.1-CONTEXT.md
@.planning/phases/02.1-ai-sales-agent-configuration/02.1-RESEARCH.md

<interfaces>
Current durable primitives available today:

From prisma/schema.prisma:
```prisma
model Message {
  id                String   @id @default(cuid())
  externalMessageId String   @unique
  direction         String
  fromPhone         String
  toPhone           String
  body              String?
  channel           String  @default("whatsapp")
  rawPayload        Json
  processedAt       DateTime?
  createdAt         DateTime @default(now())
}
```

From apps/backend/src/conversations/conversations.service.ts:
```typescript
private getStableConversationId(message: MessageRow): string {
  const participantPhone =
    this.normalizeDirection(message.direction) === 'outbound'
      ? message.toPhone
      : message.fromPhone;

  return this.normalizeParticipantPhone(participantPhone);
}
```

There is no AI module, no brief state, and no quote-draft persistence yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent AI sales domain state</name>
  <files>
    prisma/schema.prisma
  </files>
  <read_first>
    prisma/schema.prisma
    apps/backend/src/conversations/conversations.service.ts
    .planning/phases/02.1-ai-sales-agent-configuration/02.1-RESEARCH.md
  </read_first>
  <action>
1. Extend Prisma with durable models for AI sales work. The exact names may vary, but the schema must cover:
   - structured commercial brief keyed by stable conversation identity
   - quote draft with versioning
   - explicit review/approval-blocking state
2. Keep `Message` as transcript/history source of truth. Do not overload it with sales-state columns.
3. Ensure the schema can represent repeated owner feedback and regenerated drafts without overwriting the original draft irreversibly.
4. Add only the minimum relations/indexes needed for stable conversation-centric reads.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && pnpm exec prisma validate</automated>
  </verify>
  <done>
    - AI sales state is durable and queryable
    - Quote review blocking can be enforced structurally, not only by prompt wording
  </done>
</task>

<task type="auto">
  <name>Task 2: Introduce AI sales module + DeepSeek provider abstraction</name>
  <files>
    apps/backend/src/app.module.ts
    apps/backend/src/ai-sales/ai-sales.module.ts
    apps/backend/src/ai-sales/ai-sales.service.ts
    apps/backend/src/ai-sales/ai-provider.interface.ts
    apps/backend/src/ai-sales/deepseek/deepseek.client.ts
    .env.example
  </files>
  <read_first>
    apps/backend/src/app.module.ts
    apps/backend/package.json
    apps/backend/src/messaging/messaging.service.ts
    .planning/phases/02.1-ai-sales-agent-configuration/02.1-RESEARCH.md
  </read_first>
  <action>
1. Create a dedicated `ai-sales` backend module and import it from `AppModule`.
2. Define a narrow provider interface that the orchestration layer can call for:
   - brief extraction / consolidation
   - quote draft generation
   - regeneration from owner feedback
3. Implement a DeepSeek-backed client behind that interface. Keep auth/config/env handling localized there.
4. Update `.env.example` with the minimum variables required for execution.
5. Do not place LLM calls inside `WebhooksService`, `MessageProcessor`, or `MessagingService`.
  </action>
  <verify>
    <automated>cd /Users/xstaked/Desktop/projects/sn8-projects/sn8-wpp-crm/apps/backend && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - AI provider wiring is isolated and testable
    - The rest of the codebase can depend on a stable interface instead of provider-specific HTTP details
  </done>
</task>

<task type="manual">
  <name>Task 3: Capture the owner quotation format as a first-class contract</name>
  <files>
    apps/backend/src/ai-sales/prompts/quote-template.contract.ts
  </files>
  <read_first>
    .planning/phases/02.1-ai-sales-agent-configuration/02.1-CONTEXT.md
    .planning/phases/02.1-ai-sales-agent-configuration/02.1-RESEARCH.md
  </read_first>
  <action>
1. Create a dedicated quotation-template contract file that represents the owner-defined format the AI must follow.
2. If the exact template has already been provided, encode it in this contract.
3. If the exact template is still missing, stop short of hardcoding an invented template:
   - create a typed/configurable placeholder contract
   - document the missing owner input in the summary
   - do not claim `SALES-AI-03` is fully closed until the real format is supplied
4. Keep the contract separate from the system prompt so formatting can evolve without rewriting orchestration logic.
  </action>
  <verify>
    <manual>The repo contains an explicit quotation-format contract and the summary states whether the real owner template was captured or is still pending</manual>
  </verify>
  <done>
    - Quote formatting is now explicit instead of tribal knowledge
    - Later plans can generate against a contract rather than an ambiguous narrative instruction
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec prisma validate` passes after the domain models are added
2. `pnpm exec tsc --noEmit` passes after the AI sales module is wired into the backend
3. The codebase contains:
   - durable AI sales state models
   - an `AiProvider` abstraction
   - a DeepSeek-backed implementation stub or concrete client
   - an explicit owner quotation-format contract
</verification>

<success_criteria>
- SALES-AI-01 foundation exists: a dedicated premium-sales agent module can be configured
- SALES-AI-02 foundation exists: structured brief state is persistable
- SALES-AI-03 foundation exists: quotation format is explicit and the draft can be represented durably
- SALES-AI-05 foundation exists: review blocking can be enforced by state, not only convention
</success_criteria>
