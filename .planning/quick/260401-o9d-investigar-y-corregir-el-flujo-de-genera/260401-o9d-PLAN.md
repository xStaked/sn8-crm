# Quick Task 260401-o9d Plan

## Objective

Diagnosticar por que la generacion asincrona de cotizaciones no crea `QuoteDraft` en produccion y aplicar un fix minimo y seguro.

## Plan

1. Revisar el wiring de BullMQ/Nest en `ai-sales` para validar si el job realmente puede ser consumido por el worker correcto.
2. Corregir la causa raiz con el menor cambio posible y agregar observabilidad de fallo en logs del processor.
3. Ajustar el mensaje al cliente para no prometer revision interna antes de que exista un `QuoteDraft`.
4. Cubrir el routing del worker con tests y ejecutar la suite relevante.

## Notes

- Hallazgo clave esperado: el job `process-qualified-conversation` debe terminar en `AiSalesOrchestrator.processQualifiedConversation()` y no puede ser absorbido por otro worker de la misma cola.
