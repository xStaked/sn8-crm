# Quick Task 260401-p00 Plan

## Objective

Desacoplar la creación de `QuoteDraft` pendiente de revisión de la notificación legacy por WhatsApp al socio, para que el flujo CRM no dependa de `AI_SALES_OWNER_PHONE`.

## Plan

1. Confirmar el punto exacto donde `processQualifiedConversation()` falla al invocar `requestOwnerReview()` después de crear el draft.
2. Aplicar un fix mínimo en `OwnerReviewService` para que la notificación WhatsApp sea opcional y deje observabilidad explícita cuando se omita.
3. Cubrir con tests el caso sin `AI_SALES_OWNER_PHONE`, el logging de skip y la compatibilidad cuando la configuración sí existe.
4. Ejecutar la suite puntual de `ai-sales` y compilar backend para verificar que el flujo CRM siga leyendo `pending_owner_review`.

## Notes

- El contrato que debe preservarse es: `QuoteDraft` nace con `reviewStatus = pending_owner_review`, y el CRM puede leer `pendingQuote` y `quote-review` aunque no se envíe nada al WhatsApp del socio.
